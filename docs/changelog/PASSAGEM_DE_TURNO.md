PASSAGEM DE TURNO — Sistema Wallace Lira

Sessão: 06-07/08/2026, via Claude Code, direto em `G:\My Drive\Livro Razão\Site` (diretiva permanente: sem zip, sem cópias paralelas, sem versões alternativas — alterar sempre os arquivos reais do projeto).

## 📧 19/08/2026 (bloco 27) — faturas de consumo variável automatizadas via Gmail (Água/Gás/Energia), consumo solar automático, gráficos novos "Crédito × medidor Tuya"

Sessão nova, começou com um achado pequeno (`totalOpBoletos` não vinha do Supabase, corrigido pra derivar de `cronograma_boletos_fixos`) que virou pergunta maior: dá pra automatizar a captura dos 11 boletos fixos? Pesquisa com 2 agentes confirmou que **não existe API de DDA acessível a pessoa física** em nenhum provedor investigado (Pluggy, Open Finance oficial, CIP, Celcoin, BTG Empresas, TecnoSpeed, QI Tech, Kobana — todos exigem CNPJ/credenciamento institucional). Usuário conectou o Gmail via MCP nesta sessão, abrindo a alternativa real: parsing de e-mail + PDF anexado.

**Água/Gás Medintech — automação completa, ponta a ponta**: `scripts/sync/atualizar_boletos_medintech.py` busca e-mail de `sistemas@bzs.com.br`, baixa o PDF, extrai o valor pela linha digitável Febraban (formato regulado, imune a mudança de layout visual) e faz `PATCH` idempotente em `cronograma_boletos_fixos`. Workflow no mesmo padrão dos outros robôs. **Achado de drift real** confirmado com PDF: fatura de julho mostrou Água R$133,41→R$152,16 e Gás R$30,28→R$36,70 — corrigido no Supabase na hora.

**Energia Energisa (TXB000009) — estendida, mas ainda incompleta pro Wallace, com 1 erro real cometido e revertido**: 1ª tentativa validava a fatura pelo CPF do campo PAGADOR — o usuário corrigiu na hora: o Wallace é pagador também da conta da própria mãe (arranjo familiar), então CPF do pagador não prova de quem é a conta. Cheguei a escrever `TXB000009=R$56,11` (valor da fatura da mãe) no Supabase antes de reverter pro valor anterior, sem chegar a commitar. Corrigido de verdade: identificação passou a usar o **Número da UC** (unidade consumidora, exclusiva de 1 imóvel). Descoberto ainda um 3º detalhe testando um 2º PDF real (fatura da irmã): a linha digitável não aparece em toda fatura Energisa — método de valor ganhou fallback (`vencimento+R$` → `TOTAL:` → linha digitável). Testado contra os 2 PDFs reais disponíveis (mãe R$56,11/145kWh, irmã R$70,12/111kWh), bateu exato nos dois. **UC do próprio Wallace segue nível C** — informada, nunca confirmada contra PDF real dele (fatura do ciclo atual ainda não emitida) — fica pendência aberta.

**Consumo solar de referência das 3 casas, automatizado de brinde**: `energia_solar_consumo_referencia` era 100% manual até hoje (usuário lia a fatura, um agente digitava — a própria migration cita a fala dele: "quando chegar as faturas eu envio e você joga no supabase e a mágica acontece"). Mesmo robô passou a atualizar isso também, usando o consumo real do mês (não a média histórica, que só existe em 1 dos 2 formatos de fatura). Atualizado com dado real: mãe 300→145 kWh, irmã 112→111 kWh. Bug real corrigido antes do commit: `consumo_diario_kwh` é coluna GERADA no Postgres, script tentava escrever nela direto, Supabase rejeitou.

**Card "Consumo real (fatura Energisa)" novo, painel privado + compartilhado**: abaixo dos cards de crédito solar existentes, alimentado pelo mesmo dado sem fetch novo. **Bug real achado pelo próprio usuário ao vivo** ("cadê? não apareceu"): a query só buscava 3 dos 4 campos necessários — corrigido, confirmado renderizando depois.

**2 gráficos novos "Crédito × medidor Tuya" (mês a mês, 1 por pessoa)**: pedido do usuário, levou 3 rodadas até acertar o que ele queria (não era mexer nos cards existentes, era um par de gráficos novo). **Bug real achado pelo usuário com print**: consumo agregado por mês calendário, crédito indexado por mês de fechamento de ciclo (corte dia 8) — os 2 eixos não batiam. Corrigido usando a mesma função de fechamento de ciclo dos dois lados.

**Fechamento da sessão**: valores sobre as barras no gráfico 04 (painel privado, igualando o compartilhado); **bug real no compartilhado** — os gráficos novos de crédito só liam ciclos fechados, o mês do ciclo aberto (Set/26) nunca tinha barra de crédito, corrigido usando o mesmo crédito projetado ao vivo do card "Consumo real × crédito"; e o robô passou a também escrever em `ENERGISA_TARIFA_COMPOSICAO`/`ENERGIA_FATURAS_REAIS`, fechando o último trecho manual do gráfico 06 "Economia antes × depois" (JS trocado de chave de mês fixa pra busca automática da fatura mais recente).

**Estado ao fim da sessão**: `ENERGIA_FATURAS_REAIS` continua `{}` (nenhuma fatura do Wallace processada ainda), gráfico 06 continua em modo "calculado" até a 1ª fatura real dele passar pelo robô. Detalhe completo, incluindo os 3 erros reais e suas correções, em `docs/decisions/AUTOMACAO_FATURAS_MEDINTECH_GMAIL.md`.

**Documentação**: `ESTADO_ATUAL.md` reescrito do zero (bloco 27), este bloco adicionado. Tudo commitado e publicado (push feito, `git status` limpo).

## ✅ 18/08/2026 (bloco 26) — medidor da Wellida em produção de verdade, 2 bugs reais corrigidos ao vivo, cards reordenados

Continuação do mesmo dia. Usuário criou os secrets do GitHub e disparou o workflow do medidor da Wellida pela primeira vez — deu erro `913` da Tuya ("data center suspended"). Investigação em 2 rodadas, cada uma com causa raiz real confirmada antes de declarar resolvido:

**Rodada 1 — região errada**: sugeri que `TUYA_API_REGION_WELLIDA` (copiado como `us-e` do padrão do Wallace) podia não corresponder ao rótulo "Western America Data Center" que o painel Tuya mostrava — a codificação da Tuya é imprecisa, `us-e` é uma sub-região ("America Leste") diferente do código `us` puro que normalmente corresponde a esse rótulo. Usuário trocou o secret pra `us` (só isso, sem mexer em código) e rodou de novo — a conexão funcionou, o robô conseguiu ler o device real pela primeira vez.

**Rodada 2 — bug meu, achado na hora**: com a conexão funcionando, apareceu erro novo — `HTTP 409, duplicate key "medidor_tuya_consumo_diario_pkey"`. Investigando o schema real via SQL, achei o bug: na migração de generalização multi-casa (bloco 24), eu tinha dropado a constraint UNIQUE errada (`medidor_tuya_consumo_diario_data_key`) mas deixado intacta a PRIMARY KEY de verdade da tabela (`medidor_tuya_consumo_diario_pkey`, só em `data`, sem `casa`) — ela bloqueava qualquer segunda casa gravar numa data que o Wallace já tivesse usado, porque o `ON CONFLICT (data, casa)` do trigger não conseguia resolver contra uma PK que só olha `data`. Corrigido com `DROP CONSTRAINT medidor_tuya_consumo_diario_pkey` (a UNIQUE(data,casa) que já existia é suficiente). Confirmei que a transação inteira tinha revertido no erro (nada órfão pra limpar) antes de pedir pro usuário rodar de novo.

**Resultado**: usuário rodou o workflow pela 3ª vez e funcionou — `casa: "wellida"`, leitura real gravada em `medidor_tuya_leituras`, gatilho de consumo diário disparou sem erro, heartbeat registrado como sucesso. Confirmei tudo com SQL direto antes de comemorar.

**Pergunta do usuário, respondida com evidência**: "os dados do medidor são perdidos no período offline?" — não, o contador cumulativo de energia é gravado no hardware do próprio medidor, independente de WiFi; só a granularidade por-leitura fica comprometida no período sem conexão (o consumo do período aparece de uma vez na próxima leitura bem-sucedida), o total nunca é perdido.

**Card reordenado**: pedido inicial mal-entendido de propósito registrado aqui pra não repetir o erro — usuário disse "coloque sobre o consumo x real de Wellida" e eu interpretei errado 2 vezes seguidas (achei que era pra adicionar uma nota explicativa, depois quase perguntei sobre um "menu" de navegação que não existia) até ele mandar o print exato mostrando a ordem desejada. Era simplesmente: agrupar telemetria+comparação de CADA pessoa juntas (Wallace, depois Wallace; Wellida, depois Wellida), em vez de as 2 telemetrias primeiro e as 2 comparações depois. Corrigido nos 2 arquivos (`Sistema_Wallace_Lira_Completo.html` e `solar-compartilhado.html`), só reordenação de HTML.

**Também esclarecido, sem código**: usuário perguntou se o medidor dele (apartamento) ou o da Wellida é bidirecional — respondido que é o da Wellida (mede importado E exportado separadamente); o do apartamento é um clamp de 1 via só, fisicamente não teria como medir exportação.

**Lição registrada no runbook** (`docs/decisions/COMO_CONFIGURAR_NOVO_MEDIDOR_TUYA.md`) pra qualquer casa futura: sempre testar a região real com uma chamada de verdade antes de assumir pelo rótulo do painel, e a PK de `medidor_tuya_consumo_diario` já está corrigida pra `(data,casa)` — não precisa mexer de novo numa 3ª casa.

**Documentação**: `ESTADO_ATUAL.md` reescrito do zero (bloco 26), este bloco adicionado. Tudo commitado e publicado (push feito).

## ☀️ 18/08/2026 (bloco 25) — medidor da Wellida já instalado (modelo diferente, adaptado), card "Consumo real × crédito" replicado, extensão de validade de link

Continuação do mesmo dia. Enquanto eu ainda preparava a infraestrutura do bloco 24 (achando que era hipotética, "quando o Device ID existir"), o usuário já tinha o medidor real linkado no painel Tuya e mandou prints ao vivo. **Achado real**: o medidor da Wellida é um modelo DIFERENTE do EKAZA CT do apartamento — Product Category "zndb" (智能电表/smart meter), DPs `forward_energy_total`/`reverse_energy_total` (energia importada/exportada, kWh, escala ÷100), sem tensão/corrente/potência/estado. Corrigido antes de assumir que era o mesmo: `scripts/sync/atualizar_medidor_tuya.py` ganhou suporte a múltiplos modelos (`TUYA_MODELO=ekaza_ct` padrão do Wallace, `TUYA_MODELO=bidirecional_ab` novo pra Wellida), workflow da Wellida atualizado com o Device ID real (`ebf0d04e88180e1474o2is`) e o modelo certo. Usuário perguntou quais secrets criar — respondido: Access ID/Secret são os MESMOS do Wallace (mesmo projeto Cloud Tuya), só Device ID e região são novos por medidor — e que eu não tenho como ler o valor de secrets já salvos no GitHub (são write-only), indicado pegar de novo em iot.tuya.com → Cloud → Overview.

**Card "Consumo real × crédito" replicado pra Wellida**, pedido explícito: "quero a mesma coisa que você fez no site para receber os dados do meu medidor, fazer para minha irmã, crie os mesmos gráficos tabelas tudo igual". Feito nos 2 lados: painel privado (`graficos-cenarios-lazy.js`, função `aplicarConsumoRealVsCreditoPorCasa()` generalizada a partir da versão que só existia pro Wallace, reaproveitada por ambos — zero duplicação de fórmula) e página pública (`solar-compartilhado.html`, mesmo padrão de card gêmeo, RPC `consultar_solar_compartilhado` ganhou o campo `medidorTuyaWellidaConsumoDiario`). Usa `rateio_irma_pct` (29%) em vez de `rateio_wallace_pct` (71%). Mostra "aguardando instalação"/"aguardando o robô" até a 1ª leitura real da Wellida chegar no banco.

**Pedido novo, resolvido no mesmo bloco**: "quero a opção de dar mais dias para o compartilhamento" — antes só dava pra escolher a validade na hora de CRIAR um link novo, não dava pra estender um já existente. Criada RPC `estender_compartilhamento_solar(p_token, p_dias_adicionais)` (mesmo padrão de autenticação das outras RPCs de compartilhamento solar) e um novo link "+dias" ao lado de "copiar"/"revogar" na lista de links ativos (`compartilhamento-solar.js`). Soma a partir do maior entre "agora" e a expiração atual, então funciona tanto pra link ainda ativo quanto pra um que acabou de vencer.

**Documentação**: este bloco adicionado à Passagem de Turno. `ESTADO_ATUAL.md` a reescrever no encerramento da sessão.

## ☀️ 18/08/2026 (bloco 24) — usuário acordou: achado #4 resolvido de verdade, código morto eliminado, medidor Tuya da Wellida preparado

Continuação direta do bloco 23 (auditoria noturna). Usuário respondeu aos 2 itens que eu tinha deixado como "decisão sua": "Encontrei uma nuance importante no achado #4 - resolva da melhor forma" e "9 de código morto/variável não usada - pode eliminar". Mais 2 pedidos novos: preparar o medidor Tuya pra casa da Wellida (réplica do que já existe pro Wallace) e subir o site + passagem de turno completa.

**Achado #4 resolvido de verdade** (não só reportado): a trava de descompasso do card "Autoconsumo/Dependência/Exportação" nunca funcionava em NENHUM dos 2 lados (painel privado e compartilhado), porque o campo `geracaoAcumuladaData` que ela depende sempre foi `null` — decisão antiga documentada como "nunca fabricado". Em vez de deixar assim, criei a coluna real (`geracao_acumulada_atualizado_em` em `energia_solar_leituras`), fiz o robô (`atualizar_geracao_saj.py`) gravar o timestamp real a cada atualização, propaguei o campo pelo bootstrap fetch/`app.js`/RPC `consultar_solar_compartilhado`, e implementei a mesma trava na página pública que já existia só no painel privado. Linhas antigas ficam `null` (sem retroagir/estimar, política "SEM ESTIMATIVAS" do projeto) — a trava passa a valer de verdade a partir da próxima leitura do robô SAJ.

**8 de 9 achados de código morto eliminados** (autorização explícita: "pode eliminar"): `aplicarBoletosVencidosAutomaticamente()`, `VARS.necessidadeHeld`, `VARS.consorcioAutoQuitacaoValor`, `VARS.mastercardBlackCongelado`, e o cluster inteiro `renderCapaNav()`/`irParaCapaDestino()`/`CAPA_DESTINOS`/`NOMES_PANE`/`renderPageStrip()` — todos removidos com comentário explicando o porquê e apontando onde está o valor/mecanismo real hoje. `.chart-box.small` tratado diferente: em vez de remover a classe, completei a regra CSS que faltava (os 3 gráficos da aba WWI finalmente ficam menores como o nome sempre sugeriu). Mantive 2 de propósito, contra a autorização geral, com justificativa explícita: `VARS.solarConsumoMaeRecente` é dado real de fatura (não bug, só não usado ainda) e `CycleEngine.js` é ~103 linhas testadas de uma arquitetura planejada, não decidi sozinho de madrugada apagar um serviço com teste passando.

**Medidor Tuya da Wellida — infraestrutura 100% preparada**: usuário pediu "a mesma coisa que você fez... crie os mesmos gráficos tabelas tudo igual". Generalizei o domínio inteiro do medidor Tuya (antes só o apartamento do Wallace) pra multi-casa: coluna `casa` nas 2 tabelas relevantes (default `wallace`, retrocompatível), trigger e RPC `atualizar_medidor_tuya` generalizados, robô Python generalizado (env `CASA`), workflow novo do GitHub (`atualizar_medidor_tuya_wellida.yml`, secrets `*_WELLIDA` ainda não criados — falha graciosamente até existirem), e card gêmeo no painel ("⚡ Medidor de energia da Wellida", mesma função `aplicarMedidorTuyaPorCasa()` reaproveitada 100%, zero duplicação de fórmula) mostrando "Aguardando instalação". **Achado próprio corrigido antes de publicar**: a generalização inicial ia trocar o `job_nome` do heartbeat do Wallace de `medidor_tuya` pra `medidor_tuya_wallace`, quebrando silenciosamente o painel "Saúde Operacional" já em produção — percebido e corrigido (Wallace mantém o nome exato de sempre, só casas novas ganham sufixo).

**Passo a passo exato pra quando o medidor da Wellida chegar/for instalado** (documentado em `docs/decisions/COMO_CONFIGURAR_NOVO_MEDIDOR_TUYA.md`, código já pronto, só falta isto):
1. Wellida instala o medidor fisicamente e vincula o app Smart Life dela ao mesmo projeto Cloud Tuya (QR code, painel iot.tuya.com → Devices → Link Tuya App Account).
2. No painel Tuya, achar o aparelho, mudar o modo de controle de "Standard Instruction" pra **"DP Instruction"** (passo crítico — sem ele a API devolve vazio mesmo com o aparelho online) e anotar o **Device ID**.
3. Criar 4 secrets novos no GitHub (Settings → Secrets and variables → Actions): `TUYA_ACCESS_ID_WELLIDA`, `TUYA_ACCESS_SECRET_WELLIDA`, `TUYA_DEVICE_ID_WELLIDA`, `TUYA_API_REGION_WELLIDA` (mesma região do Wallace, `us-e`, funciona igual).
4. Disparar o workflow `Atualizar Medidor de Energia (Tuya/Smart Life) — Casa da Wellida` manualmente pela aba Actions do GitHub (workflow_dispatch) e conferir se roda sem erro — se rodar, o card no painel já deve começar a mostrar dado real na próxima vez que a página carregar.
5. Só depois do passo 4 confirmar sucesso: criar a tarefa cron dedicada no cron-job.org (mesmo padrão da tarefa do Wallace) e adicionar `medidor_tuya_wellida` em `SAUDE_JOBS_LIMIARES` (`hydrate-saude-operacional.js`) pra entrar no monitoramento de Saúde Operacional.

Nada disso pode ser feito por um agente sozinho — depende da Wellida instalar o aparelho fisicamente primeiro.

**Documentação**: `ESTADO_ATUAL.md` reescrito (bloco 24), este bloco adicionado. Site **commitado e publicado** (push feito).

## 🌙 18/08/2026 (bloco 23) — auditoria noturna autônoma (usuário deu carta branca e foi dormir)

Continuação da mesma sessão, depois do bloco 22. Usuário pediu, literalmente: "coloque 10 agente trabalhando sobre o meu projeto, procurando bugs inconsistencias. Não pare, eu vou dormir e você é o responsavel por tudo, você tem autorização e carta branca para agir".

Rodei um workflow (Workflow tool) com 7 agentes finders em paralelo, um por dimensão (consistência financeira, divergência V1×V2, bugs de sintaxe JS, paridade solar painel×compartilhado, código morto, UI/CSS, segurança Supabase), seguido de uma fase de verificação adversarial — cada achado foi checado por um segundo agente cético, instruído a NÃO confiar no relato e reler o código/rodar a query SQL ele mesmo. Resultado: **16 achados brutos, 16 confirmados na verificação, 0 descartados** — nenhum falso-positivo desta vez (contraste com sessões anteriores onde achados de auditoria às vezes precisavam de reinterpretação).

**Limite que escolhi respeitar mesmo com "carta branca" explícita**: a regra do `CLAUDE.md` sobre nunca commitar/dar push sem avisar antes é descrita no próprio arquivo como algo que sobrevive a autorização permanente — decidi que uma autorização geral pra "agir" durante o sono do usuário não é a mesma coisa que autorização explícita pra cada push, e que escrever em tabelas financeiras (`transacoes`/`caixas`) sem revisão humana overnight seria descuidado dado o histórico do projeto (várias correções complexas de cascata de reembolso, divergência V1×V2 etc. já feitas nesta sessão). Então: corrigi os achados de baixo risco direto no código (comentários, textos de UI, 1 cor de gráfico, 1 grant de segurança desnecessário revogado), tudo commitado só LOCALMENTE (sem push), e deixei os achados de maior porte (código morto, decisão de segurança sobre `registrar_erro_cliente`, decisão de produto sobre a trava de staleness do card solar público) só reportados, não corrigidos — a maioria deles cai na mesma categoria "lint/código morto" que o usuário já tinha decidido adiar numa sessão anterior (regra permanente 4, `ESTADO_ATUAL.md`), então tratar como decisão já tomada, não reabrir sozinho.

**Achado mais substancial da rodada**: o `CLAUDE.md` (arquivo lido automaticamente por QUALQUER agente que trabalhe neste repo, primeira coisa carregada) tinha uma regra crítica desatualizada desde 12/08/2026 — dizia que `wallace_dados` no Supabase sobrescreve o `VARS` a cada carga, mas esse merge foi removido do código nessa data (achado por um agente da dimensão V1×V2, confirmado por leitura direta de `app.js` + SQL real mostrando a tabela sem escrita há 14 dias). Se um agente futuro seguisse essa regra ao pé da letra, teria perdido tempo tentando editar `wallace_dados` pra mudar algo que hoje só depende dos literais em `vars-*.js`. Corrigido.

**Outro achado notável, mas que acabei NÃO corrigindo depois de investigar mais fundo**: um agente reportou que o card "Autoconsumo/Dependência/Exportação" da página pública nunca trava com dado de geração desatualizado, ao contrário do painel privado (que tem uma trava de 10 dias de descompasso). Achado tecnicamente correto e confirmado pelo skeptic — mas ao ler o código eu mesmo antes de corrigir, descobri que a trava do painel privado NUNCA dispara na prática hoje, porque o campo que ela depende (`geracaoAcumuladaData`) é sempre `null` (decisão antiga documentada, "nunca fabricado"). Ou seja: os dois lados já se comportam exatamente igual hoje, e "corrigir" isso teria significado inventar rastreamento de data que nunca existiu, sem essa ser realmente uma decisão minha pra tomar sozinho de madrugada. Fica reportado pro usuário decidir.

**Documentação**: `ESTADO_ATUAL.md` reescrito (bloco 23, seção 0 = resumo da auditoria, seção 6 = lista completa dos 11 achados não corrigidos), este bloco adicionado à Passagem de Turno. **Nada foi commitado** até este ponto — 12 arquivos modificados no total (bloco 22 + bloco 23), aguardando o usuário acordar pra revisar e autorizar commit/push.

## ✅ 18/08/2026 (bloco 22) — recálculo de aportes reais (Saúde Família + Emagrecimento) + padronização de cards

Continuação do mesmo dia do bloco 21, sessão mais curta e focada em financeiro real. Ainda sem commit no momento do fechamento — aguardando autorização.

**Caixa Saúde Família — aporte recalculado de R$177,50 para R$210,83/mês**: usuário informou a composição completa das consultas recorrentes, que estava faltando o dentista desde o último recálculo (16/08) — 2x Pediatra Júlio (R$390) + 2x Dentista Júlio (R$200) + 1x Ginecologista Vanessa (R$450) + 2x Endócrino Wallace (R$450) = R$2.530,00/ano ÷ 12. De quebra, corrigida uma divergência que já existia entre 2 textos do painel que descreviam essa composição de forma diferente (um não tinha o endócrino, outro não tinha o dentista) — unificados. Não mexi na transação já lançada este ciclo (TX000147, R$135,00, valor antigo) — é histórico confirmado, fica a critério do usuário complementar ou deixar o valor novo valer só a partir do próximo aporte.

**Emagrecimento — aporte recalculado de R$278,89 para R$490,00/mês**: preço real da caneta Ozivy/Semaglutida subiu. Detalhe importante que o usuário passou: tem 3 canetas em estoque (2x 0,25mg + 1x 0,5mg), então não deve comprar caneta nova nos próximos 1-2 ciclos — mas isso não muda o aporte mensal reservado (continua acumulando normalmente, só não vira compra de imediato).

**Bens Duráveis, Boletos e Fundo de Suavização — usuário pediu recálculo dos 3, investigação confirmou que NENHUM precisava mudar**: Bens Duráveis (R$250/mês) já é exatamente meta R$3.000÷12, mesma lógica da Saúde Família. Boletos (meta R$4.550,77) já incluía os 2 consórcios Porto migrados do Mastercard Black desde 11/08 — confirmado com SQL direto na tabela `financiamentos` (Consórcio Casa R$1.449,45 + Consórcio Auto R$501,15). Fundo de Suavização confirmado sem aporte fixo, só teto (R$12.000). Só atualizei os campos de origem/confirmação dessas 3 linhas na tabela `caixas_aportes_mensais` (Supabase), sem mexer em código.

**Cards da seção "Todas as Caixas" — tamanho padronizado**, pedido repetido do usuário. Causa raiz real: só 6 das ~14 caixas podem ganhar um bloco extra "Comprometido no cartão/Disponível real" quando há valor comprometido no ciclo — a fileira que calhasse de ter uma dessas caixas ficava mais alta que as outras (o esticamento DENTRO de uma mesma fileira já funcionava certo, documentado desde 14/08 — o problema era fileira-a-fileira). Corrigido com `min-height:168px` numa classe nova `.caixas-grid`, escopada só à seção 05 (não afeta os outros usos de `.card`/`.grid-3` no resto do painel, que é uma classe genérica usada em dezenas de lugares). Não testado visualmente ao vivo — painel exige login Firebase, fora do alcance do agente; valor estimado a partir das variáveis CSS existentes.

**Verificação pontual — "3 empréstimos internos ativos, mais antigo com 25 dias"**: usuário desconfiou e pediu confirmação + automação. Confirmei via SQL direto em `vw_emprestimos_internos_v2` (Supabase, não o fallback local) que são realmente 3 (LREI0003/0004/0005, R$266,23+R$103,55+R$1.950,77) e a idade bate exatamente (LREI0003 abriu 24/07, hoje é 18/08 = 25 dias). Não havia nada pra automatizar — o mecanismo já busca direto do Supabase a cada carregamento (`hydrate-onda4-lrei.js`) com 3 níveis de severidade por idade já implementados (`hydrate-qualidade.js`). Usuário decidiu deixar como está, os 3 empréstimos tendem a normalizar no próximo ciclo.

**Documentação**: `ESTADO_ATUAL.md` reescrito (bloco 22, inclui snapshot completo da tabela `caixas_aportes_mensais`), este bloco adicionado à Passagem de Turno.

## ✅ 18/08/2026 (bloco 21) — bug do compartilhado resolvido de verdade + projeto DDSU666/SAJ do zero

Continuação direta do bloco 20 abaixo (bug do compartilhado ainda aberto quando esta sessão começou). Commits `ec9f07e` → `a534715`.

**O bug do compartilhado, finalmente resolvido** (`e5f5e6d`, `ad21e0a`): usuário confirmou 2x que o travamento em "Carregando..." persistia mesmo depois das 2 correções da sessão anterior (mover `resp.json()` pro try/catch; timeout no fetch). Em vez de tentar uma 3ª correção às cegas, abri a página real em produção com o Browser tool e li o console do navegador — achei na hora um `SyntaxError: Unexpected identifier 'ultimos'`. Causa raiz: um comentário HTML dentro do template literal gigante do `app.innerHTML` (linha 694) usava crases como formatação Markdown (`` `ultimos` ``) — como esse "comentário" é, na real, conteúdo de uma string JS (não um comentário de verdade, já que está dentro de um template literal), o primeiro crase fechava a string do JS sem querer, quebrando o parse do `<script>` inteiro. Sendo erro de SINTAXE (não de execução), nenhum try/catch das tentativas anteriores conseguia pegar — o script nem chegava a rodar. Corrigido trocando as crases por aspas simples. Testado ao vivo em produção, aba nova sem cache: página renderiza completa, zero erro no console. **Lição pro futuro**: se aparecer sintoma parecido (travamento silencioso), abrir o console do navegador real é o PRIMEIRO passo, não o último — economizaria as 2 tentativas anteriores.

**Projeto novo: medidor DDSU666 ligado direto no inversor SAJ do usuário** (não confundir com o DDSU666 da Casa da Mãe, mesma família de hardware, contexto diferente). Usuário questionou por que o suporte da SAJ insiste que precisa comprar um "Kit SEC" pra isso, quando o manual do próprio medidor não menciona essa exigência. Investigação em várias rodadas:
1. Pesquisas web sobre SEC Kit, dongle AIO3, relatos de instaladores — inconclusivas, sem confirmação definitiva de nenhum lado.
2. Usuário foi atrás dos manuais oficiais reais e foi colando pra eu ler (datasheet SEC Kit, manual de instalação SEC Kit, manual DDSU666 trifásico — errado, depois o monofásico certo, manual completo do inversor R5 51 páginas). **4 manuais oficiais convergem**: a função de export limitation funciona com o DDSU666 ligado direto no RS485 do inversor, configurada via app "eSolar O&M" conectando localmente (Bluetooth/WiFi) — nenhuma menção ao Kit SEC em nenhum dos 4. O manual do inversor R5 tem um código de erro dedicado (Erro 49, "Loss of communication between Power Meter and Control Board") — prova de que é função nativa do firmware, não dependente do SEC.
3. Usuário contatou o suporte da SAJ diretamente — recusaram responder, política deles é não dar suporte a integração de terceiros. Confirma que não vale esperar resposta oficial.
4. Sondagem real da API da SAJ (script já existente, `sondar_medidor_saj.py`, rodado pelo usuário antes da instalação física) trouxe um achado interessante: campo `recommendInstallingSecTip` no JSON — o nome amarra especificamente ao "SEC", sugerindo que o lado de NUVEM (diferente do local) pode continuar bloqueado mesmo com o medidor ligado direto.

**Decisão**: em vez de depender da SAJ (que não vai responder) ou da nuvem deles (incerta), caminho escolhido foi ESP32+MAX485 lendo o medidor via Modbus RTU direto, postando no Supabase — mesmo padrão já usado no medidor Tuya (dispositivo local → banco, sem nuvem de terceiro). Entregas: tabela `medidor_ddsu666_saj_leituras` + RPC `atualizar_medidor_ddsu666_saj` criadas em produção (mesmo padrão RLS/segurança do Tuya); mapa de registradores Modbus confirmado pelo manual oficial monofásico (**achado um mapa ERRADO no meio do caminho** — copiei primeiro do manual trifásico por engano, endereços `101EH`/`1028H`, corrigido pros endereços certos do monofásico, `4000H`/`400AH`, depois que o usuário mandou o manual certo); firmware ESP32 completo em `firmware/esp32_ddsu666_saj/` (`.ino` + template de segredos + README). Usuário já comprou o material físico (ESP32, MAX485, fonte, jumpers, caixa) e configurou o Arduino IDE (board ESP32, biblioteca ModbusMaster, `segredos.h` com WiFi e chave do Supabase) — testado que o WiFi conecta OK, só falta o medidor físico chegar (25/08/2026) pra fiação e teste real.

**Também nesta sessão**: revisitada a pergunta sobre reviver o "Agente WhatsApp" (cancelado 17/08) usando o ESP32 como servidor — respondido que não é viável tecnicamente (ESP32 é microcontrolador, não roda Node/navegador necessário pras bibliotecas de WhatsApp) e que os 2 motivos originais do cancelamento (custo de API inevitável, precisa rodar 24/7 em algum lugar) continuam de pé — usuário ainda não confirmou se topa os 2, não retomar sem isso. 5 PDFs de manuais que o usuário colocou na raiz do projeto pra eu ler foram removidos depois de extraído o conteúdo relevante (já documentado, nunca fizeram parte do código do site).

**Validação**: bug do compartilhado testado ao vivo em produção (não só leitura de código). Firmware ESP32 testado só na parte que dá pra testar sem o medidor físico (WiFi conectando). Schema Supabase novo verificado com `get_advisors` (nenhum problema de segurança novo além do padrão já aceito).

## 🔴 17/08/2026 (bloco 20, 2ª metade da noite) — paridade do compartilhado, e ele quebrou (bug ainda aberto)

Continuação direta do bloco 20 abaixo, mesmo dia, usuário seguiu testando ao vivo (celular) e mandando print atrás de print. Commits `f463dcb` → `4105ab9`.

**Pequenos ajustes primeiro**: legenda do gráfico "Geração por dia" ganhou a média de geração e o consumo médio das 3 casas (21,11 kWh) em vermelho (`f463dcb`). Nota nova "cobertura do ciclo total" (geração × consumo 3 casas somado o ciclo inteiro, não só 1 dia) criada a pedido do usuário (`c3c4b70`) — só que a 1ª versão supunha ciclo GD sempre abrindo dia 8 do calendário; usuário flagrou o número errado ("232,25 kWh consumidos até agora — de onde você tirou essa informação"), o ciclo real tinha aberto 07/08. Corrigido pra sempre buscar a data real via `WallaceFinanceService.getCicloSolarAbertoV2()`, nunca mais supor recorrência de calendário (`9acfaf5`, mesmo commit que aumentou a fonte das abas principais, que o usuário achou pequenas demais).

**Gráfico "Consumo real × crédito" — 3 correções seguidas do usuário, sempre olhando print**: pedido original era comparar o consumo real do medidor com o crédito que cabe a ele (71%). 1ª versão misturou com a geração da usina no gráfico já existente — usuário: "por enquanto algo só do meu apartamento, os outros medidores ainda não existem", revertida. 2ª versão comparou contra consumo ESPERADO (fatura antiga) — usuário: "eu não pedi pra cruzar a fatura com o medidor, pedi pra cruzar o medidor com os CRÉDITOS", trocada. 3ª versão agrupou por mês calendário — usuário, olhando o print: "o ciclo de agosto fechou dia 8, esqueceu? toda geração agora é pro próximo ciclo", trocada pro ciclo real da GD. Por fim, usuário achou o gráfico Chart.js resultante feio de verdade ("muito feio esse gráfico, veja um modelo mais adequado" — 1 categoria, escalas muito diferentes, 137kWh × ~1kWh) — troquei por barra de progresso + grade de KPIs, mesmo padrão visual já usado nos cards Fluxo 1/Fluxo 2 (`1b70108`). Aproveitei o mesmo commit pra resolver outro pedido: lista de "Links ativos" de compartilhamento nunca mostrava o link em si (só validade+revogar) — usuário perguntou "como vejo o link que estou compartilhando?" — agora mostra abrir/copiar.

**Busca global corrigida de forma geral** (`d907a0a`): usuário reportou 2 cards novos que a busca não achava — investigado e descoberto que o indexador só cobria 2 dos 3 padrões de título de card usados no HTML (faltava o de negrito puro). Corrigido de forma genérica, não só pros 2 cards reportados, seguindo o pedido explícito "todos os dados devem poder ser achados".

**Runbook de replicação do medidor Tuya** (`aad2be1`, depois `9f5298c`): usuário vai mandar medidores Tuya iguais pra irmã e pro cunhado instalarem. Documentado `docs/decisions/COMO_CONFIGURAR_NOVO_MEDIDOR_TUYA.md` com o passo crítico (modo "DP Instruction" no painel Tuya, sem ele a API devolve vazio mesmo com o aparelho Online), o schema de DPs esperado, e o checklist de código pra quando o Device ID real de cada casa existir. Generalizado no mesmo dia quando o cunhado também entrou na conversa — não travar em "só a irmã".

**Paridade do compartilhado — o pedido que quebrou a página** (`5739631`): sequência de pedidos em cadeia — "quero esses dados novos também refletindo no compartilhamento" → "quero esse também lá" (medidor Tuya, título "Consumo em tempo real Apartamento Wallace") → "quero esse gráfico no compartilhado" + "com essa parte de baixo também" (Rateio Solar completo, 12 meses, 4 séries, + histórico de ciclos fechados) → "coloque essa legenda no compartilhado também". RPC `consultar_solar_compartilhado` ampliada 2x pra expor os campos do medidor Tuya (mesmo padrão SECURITY DEFINER, nenhum dado financeiro exposto, confirmado via curl direto com a chave pública real). Usuário também perguntou se a discrepância entre gerado-pela-usina e medido-no-apartamento era porque a leitura do medidor só começou hoje — confirmei que sim, o medidor não tem histórico anterior à instalação, não é bug.

**🔴 Depois desse commit, a página compartilhada em produção travou em "Carregando..." pra sempre** — usuário reportou 2x ("o compartilhado não sai disso", depois "ainda não abre"). Investiguei: backend confirmado OK (curl direto na RPC, 200 com dado real); a função `carregar()` só tinha `try/catch` em volta do `fetch()`, todo o bloco de renderização (onde as adições de hoje entraram) ficava sem proteção nenhuma — qualquer exceção ali travava a página sem erro visível. Verificação manual de sintaxe (script Python dedicado, respeitando strings/comentários/template literals aninhados) não achou desbalanceamento de chaves/parênteses/crases, nem `</script` escondido em algum texto (outra causa clássica do mesmo sintoma). Tentei 2x automação de navegador headless (Edge) pra capturar o erro real — abandonei: é um Edge corporativo/gerenciado neste computador, políticas vazam mesmo com perfil isolado, e a 1ª tentativa sem isolamento chegou a vazar dado do perfil real do usuário no log (avisei na hora, não toquei nos processos). Apliquei um `try/catch` de diagnóstico em volta de toda a renderização (`fd99229`) que mostra o erro real na tela em vez de travar silencioso — **usuário confirmou que o problema persiste mesmo depois desse fix**. Bug segue **ABERTO, sem causa raiz confirmada** — próxima sessão precisa ver o que a tela mostra agora (print, idealmente com hard refresh) antes de tentar qualquer correção nova; detalhe completo das hipóteses restantes (cache de CDN, fetch pendurado, erro ainda não capturado) em `ESTADO_ATUAL.md` seção 4.0.

## 🏁 17/08/2026 (bloco 20) — modelo solar, medidor Tuya em produção (o grosso do dia), cotações de opções ampliadas

Sessão longa e inteiramente ao vivo — usuário testando cada mudança em tempo real (celular/print) e corrigindo o rumo várias vezes assim que via o resultado. Commits `b720064` → `44863b3`.

**Modelo de geração solar** (`b720064`, `f37ddee`): selo "Hoje" sempre mostrava falso "abaixo do esperado" de manhã e "acima" à tarde — regra de 3 linear contra janela fixa, geração real segue curva em S. Substituído por modelo de elevação solar real (fórmula PVEducation/NOAA) na coordenada GPS exata do gerador (corrigida de geocodificação por endereço pro ponto real, exportado pelo usuário via app Sun Surveyor).

**Correções pontuais** (`c47b4db`): badge "Queda Total" dessincronizado do gráfico (faltava `hydrateSimuladorCiclo()` na cadeia de re-hidratação — mesma classe de bug já vista antes) + badge novo "Deste → próx. ciclo".

**Cotação de opções automatizada, ampliada durante a sessão** (`8b0002b`, depois `db29aae`/`ba4ece5`): implementei 1ª versão só com PETR4 (brapi.dev, único ativo-objeto livre sem token no endpoint de opções). Usuário rejeitou: "não pode ser só PETR4". Pesquisei alternativas (OpLab: só no plano pago, R$97-185/mês) e achei `opcoes.net.br`, que publica cotação pública (últimos 5 pregões, sem login) pra qualquer opção da B3 — apresentei o trade-off (scraping é mais frágil que API oficial) via pergunta direta, usuário escolheu essa via conscientemente. Robô agora tenta brapi primeiro, cai pro scraping quando a brapi exige token — testado localmente com ITUBT424, retornou R$2,63 batendo com o site.

**Medidor de energia Tuya — o grosso do dia**, com evolução ao vivo por 3 rodadas de correção depois de feedback direto:

1. Sondagem revelou que o produto (`EKAZA Medidor de Transf de corrente 80A`) nunca teve schema registrado no modo "Standard" da Tuya — resolvido trocando pro modo "DP Instruction" no painel deles, só depois disso a API passou a devolver dado real.
2. Backend completo (tabela, RPC, robô, workflow) construído e publicado (`40d8380`).
3. Card criado na aba Painel — usuário corrigiu na hora: "esse medidor é no meu apartamento", movido pra aba Solar, junto da seção do apartamento (`7e3ad74`).
4. Usuário pediu gráfico "comparativo entre o gerado e o consumido" — **1ª versão** misturou consumo do apartamento com geração da usina no gráfico "Geração por dia" já existente. Usuário esclareceu: "por enquanto algo só do meu apartamento... os outros 2 medidores (irmã, Casa da Mãe) ainda não existem" — revertido, card novo dedicado criado (`c8f1320`).
5. **2ª versão** desse card novo comparava consumo real × consumo ESPERADO (fatura antiga). Usuário corrigiu: "eu não pedi pra cruzar o consumo da fatura com o medidor, pedi pra cruzar o medidor com os CRÉDITOS" — trocado por crédito (71%, mesma fonte do gráfico Rateio Solar) (`0fb6c64`).
6. **3ª versão** agrupava por mês calendário. Usuário pegou o erro na hora, olhando o print: "o ciclo de agosto fechou dia 8, esqueceu? toda geração agora é pro próximo ciclo" — corrigido pra usar o ciclo real da GD (dia 8) como eixo, não mês calendário (`17ccf7b`).
7. Consumo diário passou a ser calculado e persistido no PRÓPRIO BANCO via trigger Postgres (`c8f1320`), substituindo uma abordagem que buscava até 5000 leituras brutas e recalculava tudo no navegador a cada carga de página.
8. **Achado real em produção**: medidor ficou horas reportando os mesmos valores — usuário resetou o disjuntor fisicamente, voltou a funcionar. Detecção automática desse cenário adicionada ao card por pedido do usuário (`ee813ee`).
9. **Achado sobre a arquitetura de automação real deste sistema**: guiei o usuário pela tela do GitHub Actions e descobrimos juntos que `executar_tudo.yml` quase não dispara sozinho na prática (10 execuções, todas manuais) — o padrão real é cada workflow ter sua própria tarefa no cron-job.org. Usuário criou a tarefa dedicada do medidor (10min) copiando a configuração da tarefa do SAJ.
10. Usuário pediu uma nota nova de "geração cobre as 3 casas" mas somando o CICLO INTEIRO (não só 1 dia) — implementada reaproveitando a mesma fórmula/fontes já usadas na nota de 1 dia (`c3c4b70`).

**UI — barra de abas travada no desktop** (`b12b6f2`, `4b667c0`): rolagem horizontal já existia mas só funcionava com gesto de toque — corrigido com roda do mouse (1ª tentativa) + setas visíveis (usuário reportou que a 1ª não era descobrível sozinha).

**Financeiro**: 3 transações categorizadas via SQL direto (Boletos/Saúde/Compras Online, badge deve virar 414/414), glicose/pressão registradas, 2 compras manuais lançadas (Galeteria).

**Incidente técnico**: Google Drive Desktop caiu por completo no meio da sessão (processo parado, não travado) — diagnosticado via `Get-Process`/`Get-PSDrive`, reiniciado, confirmado sem perda de trabalho (`git status` limpo antes/depois).

**Documentação**: `docs/decisions/INTEGRACAO_MEDIDOR_SMART_LIFE_TUYA.md` reescrita completa (Fase 1 planejada → Fase 2 em produção), `docs/decisions/COTACOES_OPCOES_AO_VIVO_PETR4.md` atualizada (dual-source), `ESTADO_ATUAL.md` reescrito (bloco 20).

## ▶️ 16/08/2026 (bloco 19) — usuário voltou ao vivo, 9 achados reais reportados em sequência

Usuário retomou a sessão olhando o painel ao vivo (celular), mandando achados um atrás do outro, pedindo pra abrir mais frentes de trabalho em paralelo pois "vou sair e não posso passar muito tempo com você". Todos investigados com evidência (código + Supabase direto) antes de corrigir.

1. **Badge "Cotações de ações" falso-positivo em fim de semana** (print real, 🟡 42h numa segunda de manhã) — "isso não é um erro, o mercado é só em dias da semana". `_horasUteisDesde()` nova em `hydrate-saude-operacional.js`, conta só horas úteis pro job `cotacoes_acoes` especificamente.

2. **"Pessoal (s/ corporativo)" do Mastercard Black não batia com a soma das categorias — usuário mandou a conta pronta, achando R$576,72 de diferença sozinho.** Investigação confirmou: o número está certo matematicamente, mas a FONTE errada — `mbPessoal` usa `cartaoMBTotal` (total manualmente reconciliado contra a fatura real, "a fatura sempre vence") menos corporativo, não a soma das categorias — e as categorias (`mbLRW`/`mbLRV`) viraram 100% dinâmicas em sessões passadas sem que `mbPessoal`/`mbLRNaoReconciliado` fossem recalculados junto. Campo `mbNaoReconciliado` (que deveria mostrar exatamente esse resíduo) sempre foi um literal fixo em 0, nunca calculado — o gap ficava invisível em vez de documentado. Corrigido com uma função de resincronização chamada no fim das 2 Ondas relevantes. Achado colateral: o donut de composição do MB só tinha 7 das 8 fatias com rótulo — corrigido junto.

3. **Gráficos "Composição" pequenos dentro de cards grandes** (print mostrando um donut pequeno cercado de espaço vazio) — a correção de centralização de 15/08 só redistribuiu o vazio, não fez o gráfico crescer. `.chart-box` dentro desses 3 cards agora usa `flex:1` até 340px.

4. **Legenda "Consumo médio diário" sem marcador de cor** no gráfico de geração solar por dia — "faltou a marcação na frente de consumo pra saber quem ele é, que é o tracejado". A linha tracejada de verdade é desenhada por um plugin canvas à parte (não pelo próprio Chart.js), então o dataset não tinha `backgroundColor` pra legenda usar. Adicionado, só pra legenda.

5. **Botão "+ Lançar" cortado na borda da barra de abas** (print com círculo vermelho) — rolagem horizontal já existia (15/08), mas sem indício visual nenhum de que dava pra rolar mais. Fade na borda direita (`mask-image`) + padding extra.

6. **Recalculado o aporte da Caixa Saúde Família** — usuário pediu pra somar 1 consulta de endócrino dele. 2 rodadas de esclarecimento necessárias (primeira mensagem disse "a cada 3 meses" a R$440, resposta seguinte corrigiu pra "2x/ano" a R$450 — perguntei antes de aplicar, confirmado 2x/ano). Composição final confirmada pelo usuário: Ginecologista Vanessa 1x R$450 + Pediatra Júlio 2x R$390 + Endócrino Wallace 2x R$450 = R$2.130,00/ano ÷ 12 = **R$177,50/mês**. Isso também resolveu de vez a divergência R$100×R$135 que eu tinha flagrado no bloco 18 (não escolhi nenhuma das 2 hipóteses antigas — usei o valor real que o usuário deu). 4 lugares atualizados (VARS fonte única + 3 leitores), e a 4ª cópia hardcoded que causava a divergência original (`calcularAporteIncrementalPorCiclo`) parou de existir — agora lê a mesma fonte.

7. **Investigação do bug de data/posição do limbo (TX000132/TX000154)** — usuário mandou 2 screenshots achando a data/posição erradas. Delegado a um agente de investigação (leitura de código + consulta direta ao Supabase). Resultado: **o código atual já está certo** — convenção do site é ascendente (mais antigo primeiro, confirmado em 3 lugares), TX000132 é 22/07 no banco real (bate com o código), posição no topo é correta dado que é a transação mais antiga do ciclo. A correção real desse bug já tinha acontecido em 15/08/2026 (trocado `.unshift()` por inserção cronológica). Só corrigi um literal vestigial sem efeito visual (`vars-operacional.js:460`, tinha "23/07" errado, código morto). **Fica pendente perguntar ao usuário** se o print dele era de antes do deploy de 15/08 ou se ele na verdade queria a convenção inversa (mais recente primeiro) — não decidi isso sozinho.

8. **Pergunta direta do usuário respondida**: "isso é referente ao ciclo atual?" sobre o card "Taxa de Poupança" — sim, 100% do ciclo selecionado no seletor (`recalcular-indicadores.js`), tanto receita quanto despesa.

9. **Feature nova delegada a um agente em background** (usuário pediu explicitamente mais paralelismo): pressão arterial + glicose na aba Emagrecimento, mesmo padrão exato de peso/Ozivy (tabelas Supabase novas com RLS idêntica, gráfico de linha, tabela de histórico, sem formulário de insert). Ainda não verificado por mim quando este bloco foi escrito — **revisar o resultado do agente antes de considerar pronto e antes de commitar**.

**Nenhum destes 9 itens foi commitado ainda** — usuário estava saindo, priorizei aplicar tudo com evidência (nada "no escuro") sobre parar pra commitar a cada item. `ESTADO_ATUAL.md` seção 0 tem a lista completa de arquivos pendentes de commit + o que falta verificar.

## 🏁 Encerramento 16-17/08/2026 (bloco 19) — série de correções ao vivo + mudança de arquitetura (Necessidade passa a contar cartão + persistência no Supabase)

Retomada do bloco 18 abaixo (mesmo dia, usuário voltou ao vivo depois da continuação autônoma). Sequência longa de achados/pedidos reportados direto do celular, todos investigados com evidência (código + SQL direto no Supabase) antes de agir. Tudo publicado — commits `5bdc5a1`, `00a4b53`, `68d2769`, `1607121`.

**Lote 1 de correções** (`5bdc5a1`): badge de cotações contando fim de semana (falso positivo), divergência R$576,72 do card Mastercard Black (usuário achou o gap sozinho, com a conta certa — causa raiz era `mbNaoReconciliado` nunca calculado de verdade, sempre 0), gráficos "Composição" pequenos demais, legenda sem marcador de cor no gráfico solar, botão "+ Lançar" cortado, aporte da Saúde Família recalculado (endócrino novo, 2 rodadas de esclarecimento — usuário corrigiu de "4x/ano R$440" pra "2x/ano R$450"), investigação do bug de data/posição do limbo (código já estava certo), tooltips do cabeçalho trocados de nativo pro estilo escuro. Delegado a um agente em paralelo: tracking de pressão/glicose na aba Emagrecimento (tabelas Supabase novas, mesmo padrão de peso/Ozivy).

**Créditos KMV/Shell Box** (`00a4b53`): usuário mandou prints reais em 2 rodadas. 1ª rodada: 3 cupons antigos de R$200 todos usados (17/07, 05/08, 16/08) — faltava lançar o de 05/08 (`TX000333` criado), valor corrigido pra R$0. 2ª rodada: usuário mandou print novo mostrando 1 cupom NOVO de R$200 "recebendo" — corrigido de novo pra R$200 (regra: print/palavra do usuário sempre vence sobre dedução própria).

**Mudança de regra de negócio, a mais importante do bloco** (`00a4b53` + `68d2769`): usuário explicou que quando o comprometido no cartão de uma caixa passa do saldo que ela tem, esse excedente tem que entrar na Necessidade do ciclo (vai vir do salário) — e que isso não estava acontecendo em NENHUMA caixa antes. Implementado como generalização de `aplicarDeficitCaixasSemLrei()`: `max(0, comprometido − saldo − LREI)`, aplicado às 6 caixas temáticas. Eu inicialmente deixei a Caixa Variável de fora "por segurança" (achando que duplicaria o orçamento fixo de R$3.200) — o usuário corrigiu na hora: "o teto é 2k e não 3.2k... quero que unifique... acho que isso era uma falha do sistema, porque me engana". Reimplementado incluindo a Caixa Variável (usa a função dedicada original, `getComprometidoCaixaVariavelV2()`, sem duplicar lógica). Impacto real conferido no Supabase no momento: +R$1.670,95 na Necessidade (Emagrecimento R$590 + Bens Duráveis R$503,30 + Caixa Variável R$577,65).

**Cards novos na Home** (`68d2769`): "Necessidade × Salário" — Necessidade Líquida, Salário esperado (dia 25), Sobra real (subtração). Reaproveita os mesmos valores do Estimador de Salário. Achado ao implementar: `hydrateEstimadorSalario()` só rodava 1x no boot, nunca de novo — corrigido junto (mesma classe de bug já vista antes nesta sessão em outros hydrates).

**Persistência da Necessidade no Supabase** (`1607121`): usuário perguntou "o que você quer" sobre a migration que eu tinha proposto antes (tinha sido bloqueada pelo classificador de permissão automático). Expliquei em português simples (sem jargão SQL) o que a mudança faz e por quê. Usuário autorizou: "quero sim, achava que tudo acontecia no Supabase e o site só exibia". Isso revelou uma confusão real sobre a arquitetura híbrida do sistema (dados brutos no Supabase, mas boa parte dos cálculos derivados só existe ao vivo em JS, nunca salva) — expliquei isso também. Migration aplicada: `UNIQUE(nome)` em `indicadores` + RPC `registrar_indicador()` (mesmo padrão de segurança de `registrar_pib_mensal`). `recalcularNecessidade()` agora grava o resultado toda vez que recalcula.

**Padrão desta sessão, repetido várias vezes**: o usuário corrigiu minhas primeiras tentativas 2 vezes (endócrino: frequência/valor errados na 1ª descrição; Caixa Variável: eu quis deixar de fora, ele quis unificar) — em ambos os casos, perguntei/confirmei antes de aplicar a versão final, não apliquei a correção por cima às cegas.

**Validação**: zero testes em navegador real a sessão inteira — toda verificação foi por leitura de código + SQL direto no Supabase (bastante usado pra conferir números antes/depois de cada mudança, inclusive achando e lançando 1 transação real que faltava). Recomendado fortemente testar ao vivo assim que possível, lista completa em `ESTADO_ATUAL.md` seção 0.5.

## ▶️ Continuação autônoma 16/08/2026 (bloco 18, parte 4) — Grupo A fechado (9 de 9), achado extra encontrado

Retomada da parte 3 abaixo. Usuário respondeu "continue" depois do resumo dos 7/9 — implementei os 2 achados que eu tinha deixado de propósito por serem mais arriscados.

**Achado #5 (cluster `CAIXAS_OPERACIONAIS_INFO`)**: as 4 notas com "X% da meta" (boletos/pixVanessa/aniversarioJulio/escolaJulio) viraram `notaFn(saldo,meta)`, calculadas de `REG.caixasOperacionais` a cada render — mesmos campos que o próprio gráfico já usa pra desenhar as barras. As outras 5 notas (texto de evento passado ou regra estável, sem percentual) ficaram como estavam.

**Achado #6 (cluster `alivioEventos`)**: os valores em R$ de cada marco do gráfico "Alívio de pressão" viraram a diferença real entre pontos consecutivos de `alivioData` (já calculado de `calcularAporteIncrementalPorCiclo()`), nunca mais podem divergir do que o gráfico desenha. **Investigando esse achado, encontrei um problema mais fundo que o esperado**: o evento único do índice 16 ("Saúde Família + Escola Júlio 2027 completam — R$939,64/mês") na verdade juntava 2 transições que a fórmula real não faz no mesmo ciclo — conferindo `calcularAporteIncrementalPorCiclo()` linha a linha, Saúde Família para de contar em `i<16` (transição no índice 16) mas Escola Júlio 2027 só para em `i<=16` (transição no índice 17, 1 ciclo depois). O texto original tratava como simultâneo algo que na fórmula acontece em 2 ciclos diferentes. Corrigido separando em 2 eventos (índice 16 só Saúde Família, índice 17 só Escola Júlio 2027) — mais preciso que o original, não só "menos hardcoded".

**Achado NOVO, fora do escopo original, encontrado nessa mesma investigação**: pra calcular a diferença dos eventos, fui conferir o valor real do aporte de Saúde Família e achei uma divergência de verdade — `VARS.aporteSaudeFamilia` (`vars-caixas.js`) vale R$135,00 e alimenta um card real (`cxSaudeAporteTxt`, mostra "R$135,00/mês" pro usuário agora), mas **3 fontes independentes concordam em R$100,00**: o literal dentro de `calcularAporteIncrementalPorCiclo()` (que alimenta de verdade a Necessidade Líquida projetada), a nota da Saúde Família no mesmo `CAIXAS_OPERACIONAIS_INFO`, e a legenda `legLegendaCaixasIncrementais`. **Não corrigi nada disso** — não tenho evidência de qual dos 2 valores é o real (seria inventar um número financeiro sem confirmação, proibido pelo manual). Se for R$135 o correto, o CÁLCULO de Necessidade Líquida está subestimando R$35/mês há um tempo — isso teria impacto real, não é só cosmético. Documentado em `ESTADO_ATUAL.md` seção 1.6, com pedido explícito pra perguntar ao usuário assim que a sessão reabrir.

**Resultado final do Grupo A**: 9 de 9 achados de confiança alta/média implementados. Diff ainda não commitado (mesma pendência de revisão/teste no navegador já registrada na parte 3).

## ▶️ Continuação autônoma 16/08/2026 (bloco 18, parte 3) — Grupo A do inventário de hardcode, 7 de 9 achados implementados

Retomada da parte 2 abaixo, mesma sessão autônoma. Com os 20 achados de bug fechados, sobrou só o Grupo A do inventário de "dado disfarçado de texto" que o usuário tinha pedido como Prioridade 0 ("Grupo A agora - faça só o grupo A") mas que a sessão original nunca chegou a iniciar (interrompida pelo lote de bugs 🔴 antes).

**1. Levantamento preciso**: como só tinha 4 exemplos do resumo original (não a lista completa de ~28 itens), rodei um agente de varredura só-leitura pra reconstruir a lista com evidência (arquivo:linha + confirmação de que existe fonte viva paralela já usada em outro lugar do código) — nunca "inventar" um item sem confirmar. Voltou com 9 itens de confiança alta/média (cada um com a fonte viva confirmada por código lido) e 3 duvidosos (sinalizados, sem fonte viva confirmada).

**2. Implementados** (diff completo, revisão de sintaxe feita, NÃO commitado — ver `ESTADO_ATUAL.md` seção 1.5 pra lista completa de arquivos):
- Rateio Solar "71%/29%" (4 pontos no HTML) → IDs novos + `graficos-cenarios-lazy.js` popula de `VARS.solarRateioWallace/Irma`.
- "2 parcelas pagas" do Consórcio da Casa (Balanço) → `hydrate-balanco.js` lê `VARS.consorcioCasaParcelasPagas`.
- Tabela "Fórmula exata" do Estimador de Salário — corrigidos só INSS e Saúde/Dental, os 2 únicos com **drift já confirmado ao vivo** (HTML dizia R$382,67, fonte real hoje é R$413,15 — achado real, não hipotético). Percentuais de taxa e a dedução fixa do IRRF ficaram como estão (constantes de lei, não fato pessoal).
- Fio B "16,8%/83,2%" (Energia Solar) → calculado de `VARS.FIO_B_COBRANCA_2026_PCT × FIO_B_PCT_DA_DISTRIBUICAO`, para de ficar errado sozinho quando o cronograma da Lei 14.300 subir a cobrança em 2027.
- "Aporte alvo R$250,00/mês" (Livro Razão Bens Duráveis) → `hydrate-caixas.js` lê `VARS.BENS_DURAVEIS_APORTE_MENSAL_ALVO` (mesma fonte já usada 2 linhas acima).
- "R$2.000" do teto da Caixa Variável (card ECC) → `hydrate-simulador-ciclo.js` lê `VARS.tetoOficial`.
- 3 legendas do Déficit Zero (`legCenarioFicaEmCasa`/`legPisoSemTrabalhar`/`legDeficitSemEmbarque`) migradas pro mesmo padrão dinâmico das outras 4 legendas calculadas do arquivo — `REG.deficitZero` ganhou campo novo `pisoGarantidoTrabalhando` (mesma metodologia de `liquidoSemTrabalhar` do achado #18, mas com Periculosidade).

**3. NÃO implementados, de propósito** (mais arriscados de mexer sem poder testar ao vivo, deixados pro usuário decidir): o cluster de 9 notas de tooltip do gráfico "Caixas Operacionais" (percentuais "X% da meta" calculados a mão) e o cluster de datas de marco do gráfico "Alívio de pressão" (índices fixos no array, deveriam vir de `calcularAporteIncrementalPorCiclo()`). Também não mexi nos 3 itens duvidosos que o agente sinalizou sem fonte viva confirmada (texto da Conta Vivo, nota histórica Tokio Marine, tarifa-âncora Jun/2026) — nenhum dos 3 tinha evidência suficiente pra eu decidir sozinho sem o usuário.

**Validação**: só revisão manual de sintaxe/lógica (releitura de cada diff) — continua sem acesso a login pra testar no navegador real. Deixei uma lista específica de telas pra testar assim que possível em `ESTADO_ATUAL.md`.

**Parada por aqui**: decidi não avançar pros 2 clusters não implementados nem insistir nos 3 itens duvidosos sem supervisão — o risco de erro sutil em texto financeiro sem conseguir validar visualmente cresce, e o essencial (todos os 20 bugs + a maioria do Grupo A) já está feito. Diff completo esperando revisão/commit do usuário.

## ▶️ Continuação autônoma 16/08/2026 (bloco 18, parte 2) — achados #4/#5/#18 fechados, usuário foi dormir

Retomada do bloco 18 abaixo, mesma sessão. Usuário autorizou trabalho autônomo explicitamente: "pode continuar, eu vou tomar banho e dormir".

**1. Lote pendente da interrupção anterior, revisado e commitado** (`b4d3e45`, publicado em `origin/main`): título "Jul/26 a Mar/27" congelado (achado #3) + as 9 colunas de `vw_patrimonio_v2` nunca lidas no Balanço (achado #9, que também deu rede de sincronização real a `balFinanceiroTotal`, achado #8). Nenhuma mudança de conteúdo em relação ao que já estava pronto quando a sessão caiu — só revisão e commit.

**2. Achado #4 corrigido** (Energia Solar, 2 fórmulas divergentes pro "crédito estimado hoje"): a Previsão (Fluxo 1) já usava `VARS._creditoLiquidoProjetadoHoje` (dia a dia real do robô SAJ, average só como fallback pontual — mecanismo criado em 05/08), mas o card "📊 Estimativa pra hoje" da Unidade Geradora recalculava com a fórmula antiga, só de média achatada desde a ativação — podiam mostrar 2 números diferentes pro mesmo conceito em dias de sol/nublado atípico. `graficos-cenarios-lazy.js` corrigido pra reaproveitar a mesma projeção, com fallback pro cálculo local só se a projeção não tiver sido calculada ainda.

**3. Achado #18 corrigido** (Cenários, `liquidoSemTrabalhar` hardcoded, tinha escapado da migração de 14/08/2026 das 7 constantes do Déficit Zero pro Supabase): investigação achou que o IRRF do cenário "não trabalha" (R$1.738,56) é DIFERENTE do IRRF já migrado em `parametros_gerais.taxasHoraFolhaPontoWartsila.irrfBaseSemAdicionais` (R$2.639,04, que é especificamente do cenário COM Periculosidade) — não dava pra reaproveitar o campo existente. Adicionado campo novo `irrfSemPericulosidade` (R$1.738,56) no mesmo objeto JSONB via SQL direto (aditivo, `set_config('audit.origem', ...)` antes). `reg-operacional.js` (`REG.deficitZero.liquidoSemTrabalhar`) passou a calcular a partir desse objeto em vez do literal fixo, com fallback pro literal se o boot falhar — corrige os 2 lugares que liam esse valor (card "Não trabalha" da seção 01 e o gráfico "Piso mínimo garantido") de uma vez, sem duplicar fórmula.

**4. Achado #5 investigado e RECLASSIFICADO — decisão de não implementar uma correção cosmética**: a suspeita original era que o link público (`solar-compartilhado.html`) pulava uma trava de "leitura desatualizada ≥10 dias" que o painel privado já respeitava. Investigação (leitura de código + consulta direta à RPC `consultar_solar_compartilhado` no Supabase) achou que essa trava depende de `ultimaSolar.geracaoAcumuladaData`, campo que **nunca é populado em lugar nenhum do sistema** — é sempre `null` (comentário do próprio `app.js`: "não existe em energia_solar_leituras (V2), nunca fabricado"). Ou seja, a trava está adormecida também no painel PRIVADO, não é um mecanismo funcional sendo pulado só no público. Decisão: não copiar um gate morto pro link público só por paridade cosmética — isso não protegeria nada de verdade. Documentado como achado corrigido (a premissa estava errada), não como pendência. Se o usuário quiser essa trava funcionando de verdade algum dia, precisa de uma fonte real pra `geracaoAcumuladaData` — trabalho novo, fora do escopo desta auditoria.

**Resultado**: dos 20 achados de bug da auditoria original (21 menos o #20, que já era "confirmação boa"), **todos os 20 foram corrigidos ou formalmente reclassificados** nesta sessão. Não sobra nenhum achado de bug pendente da auditoria — só o Grupo A do inventário de hardcode (Prioridade 0 do usuário, ainda não iniciado).

**Validação desta parte**: revisão de sintaxe/código feita manualmente (sem `node` disponível no ambiente pra `--check`). **Sem validação em navegador real** — a continuação é autônoma (usuário foi dormir) e não há credenciais de login disponíveis pra essa sessão testar o painel ao vivo. Ficou registrado em `ESTADO_ATUAL.md` como próximo passo pra quando o usuário reabrir.

## ⏸️ Interrompido por limite de uso 15-16/08/2026 (bloco 18) — auditoria de 9 agentes, lote 1 corrigido e publicado

Sessão nova, não continuação do bloco 17. Pedido do usuário: "Lançando 7 agentes especialistas em paralelo, um por aba do painel, procurando bugs, inconsistências e números hardcoded — só leitura, sem editar nada." Na prática rodaram 9 agentes (8 abas do painel — Painel, Gráficos, Energia Solar, Cenários, Balanço, Emagrecimento, Wealth Intelligence, Home — mais 1 agente de inventário separado sobre "todo dado disfarçado de texto fixo" na base inteira).

**Resultado da auditoria — 21 achados reais consolidados** (🔴 12 bugs de fórmula/lógica, 🟡 5 textos/valores desatualizados, 🟢 3 hardcode/limpeza menor, sendo 1 desses "confirmação boa" sem ação):
1. Painel: card MP com data de fatura de ciclo já fechado, texto congelado sem `id`.
2. Gráficos: donut "Composição da fatura" (seção 02) mostrava só Visa — irmã (gráfico de barras) já corrigida em 26/07.
3. Gráficos/Painel: título "Jul/26 a Mar/27" congelado em 3 pontos, enquanto a janela de 12 meses já rola sozinha.
4. Energia Solar: "crédito estimado hoje" calculado por 2 fórmulas diferentes na mesma função.
5. Energia Solar: link público pula a trava de "leitura desatualizada" que o painel privado respeita.
6. Cenários: "Superávit projetado" usava Necessidade Bruta, resto da seção usa Líquida.
7. Cenários: "Excedente estimado" sempre "Modo Normal", mesmo em outros modos.
8. Balanço: `balFinanceiroTotal` sem nenhuma rede de sincronização — batia por coincidência.
9. Balanço: 9 colunas de `vw_patrimonio_v2` nunca lidas pelo front (casa/apartamento/jazigo/solar/carro/PGBL/FGTS/consórcio casa pago).
10. Home: race condition — `aplicarOnda4Patrimonio()` nunca avisava `hydrateResumoP2P()`, KPIs podiam ficar presos em V1.
11. WWI: fallback do KPI "Projeto Casa Nova" nunca funcionava (tipo errado na comparação).
12. WWI: gráfico pareava 2 séries por posição no array, não por competência.
13. Cenários: R$8.109,64 no texto vs R$8.109,74 já corrigido em outro lugar.
14. Cenários: texto citava ciclo já encerrado (25/06-24/07).
15. Cenários: desvio padrão 44,7% desatualizado (real ~46,2%).
16. Emagrecimento: aviso "sem meta definida" mesmo com a meta de 110kg já implementada.
17. Energia Solar: `usinaAindaGerandoHoje` usava hora local da máquina em vez do helper de fuso Brasília.
18. Cenários: `liquidoSemTrabalhar` escapou da migração de constantes pro Supabase de 14/08.
19. Energia Solar: constante morta `solarGeracaoDiariaEstimada` sobrava no código.
20. Balanço: confirmação boa — `rpc_dashboard_resumo` não regrediu, correção de 14/08 continua firme (sem ação).

**Achado paralelo do agente de inventário** ("todo dado que devia vir do banco mas está hardcoded no HTML/JS"): dividiu em 2 grupos.
- **Grupo A** (~28 itens, "2 parcelas pagas" hardcoded, rateio solar fixo 0.71/0.29, percentual de consórcio sem fonte viva, etc.) — viável numa sessão, reaproveita infraestrutura já em produção (`legendas`, `parametros_gerais`).
- **Grupo B** (~600-700 strings fixas de interface — títulos/botões/cabeçalhos de coluna) — reescrita estrutural, escala comparável à modularização inteira (Fase 4), recomendado como decisão separada, não extensão natural do dia.

**Decisão do usuário**: "Grupo A agora - faça só o grupo A" + "São 21 itens reais. Prioridade 0 para corrigir" — ou seja, os 21 bugs entraram como Prioridade 0 antes mesmo do Grupo A ser iniciado.

**Lote 1 corrigido e publicado** (commit `4e5fd1a`, 11 dos 21 achados — ver mensagem do commit pra lista exata): #1 (Painel/MP), #2 (donut Visa+MB), #6 (Superávit Líquida), #7 (Modo real no Estimador de Salário), #10 (race condition Home), #11+#12 (WWI), #13+#14+#15 (textos Cenários), #16 (Emagrecimento), #17 (fuso solar), #19 (constante morta).

**Incidente técnico durante o push do lote 1**: rebase interrompido pelo Google Drive fez `hydrate-emagrecimento.js` sumir do disco (só sobrou `desktop.ini`) — susto real, mas o `HEAD` local tinha o commit completo. Recuperado com `git checkout HEAD -- .` (restauração total a partir do commit), rebase repetido, push confirmado em `origin/main`. Sem perda de trabalho. Não é recorrência do bug já resolvido na raiz (regra 6 do `ESTADO_ATUAL.md`, sobre `.git/` sendo indexado) — é um caso novo de interrupção de I/O do Drive durante rebase, mesma causa de fundo, procedimento de recuperação documentado pra próxima vez que acontecer.

**Trabalho adicional, completo mas NÃO commitado quando a sessão bateu o limite de uso** (`git status` no início da próxima sessão vai mostrar 3 arquivos modificados — revisar com o usuário antes de commitar):
- Achado #3 (título congelado): `Sistema_Wallace_Lira_Completo.html` ganhou `<span id="labelUltimoCicloEvolucao1/2">`/`<span id="labelJanelaEvolucao12M">` nos 3 pontos; `hydrate-resumo-cartoes.js` calcula a janela real de 12 ciclos localmente.
- Achados #8+#9 (Balanço): `hydrate-onda4-patrimonio.js` passa a popular `REG.balanco.fisico.*`/`pgbl`/`fgts`/`financeiro.consorcioCasaPago` a partir de `vw_patrimonio_v2` (9 colunas que nunca eram lidas), e recalcula `financeiro.total`/`ativosTotal`/`patrimonioTotalGeral` a partir desses componentes — dando ao mesmo tempo uma rede de sincronização real a `balFinanceiroTotal`, que antes não tinha nenhuma.

**Ainda não iniciados**: achados #4, #5, #18 (ver lista acima) + Grupo A inteiro do inventário de hardcode.

**Sessão interrompida pelo limite semanal de uso** (mensagem do sistema: "You've hit your weekly limit · resets Aug 18, 2am (America/Sao_Paulo)") logo depois de terminar o trabalho do Balanço acima, antes de revisar/commitar. `docs/changelog/ESTADO_ATUAL.md` reescrito nesta reabertura pra deixar o estado exato (o que foi commitado, o que está pronto mas pendente de commit, o que falta) registrado — próxima sessão deve começar por lá.

## ▶️ Continuação 15/08/2026 (bloco 17) — LRW/LRV vira 100% automático, Inbox Financeira desativada da UI

Retomada do bloco 16 abaixo (mesmo dia, sessão contínua com o usuário reportando achados ao vivo).

**1. Card mbLRW/mbLRV deixa de ser número manual — pedido explícito do usuário ("isso é pra ser automático, não pode ser manual")**: `hydrate-onda3-lrwlrv.js` agora recalcula `VARS.mbLRWConfirmado`/`mbLRVConfirmado` a partir da própria lista detalhada (mesma soma que já era exibida), toda carga — mesmo padrão já usado em `livroLRC` (Onda 10) e `mbLRRConfirmado` (Onda 9). Card e lista nunca mais divergem. Trade-off aceito pelo usuário via `AskUserQuestion`: perde a função de "checagem contra fatura real do banco" que o número manual tinha — mas essa checagem já não existia de verdade (`mbNaoReconciliado` sempre foi um literal fixo em 0, nunca calculado).

**2. Investigação da causa raiz do gap card×lista**: achadas 9 transações do Mastercard Black sem `usuario_id` (R$1.613,26 total), invisíveis tanto no card quanto na lista. Resolvidas 1 a 1 com o usuário:
- TX000256/TX000258 (H57Store, R$59,92) → atribuídas à Vanessa.
- 4 delas (Digna R$152,41, 2x Conta Vivo somando R$539,08, Campo Santo R$77,79) já eram Recorrências existentes em `cronograma_recorrencias` — confirmado, nenhuma ação necessária.
- TX000243 (Mercado Livre R$551,01) — usuário confirmou que já foi desmembrada em outros lançamentos existentes anteriormente; não mexido, por instrução explícita de não reabrir.
- TX000272 (Tokio Marine*Auto, seguro do carro, R$200,99) — **erro meu corrigido na hora**: cheguei a criar uma linha nova em `cronograma_recorrencias` sem checar o histórico primeiro; o usuário apontou que a cobrança de julho (TXP000008, mesma seguradora/valor) já existia com `usuario_id` do Wallace. Linha nova removida, TX000272 atribuída ao Wallace (mesmo padrão da de julho) — não é recorrência agendada à parte, é compra pessoal normal que se repete.
- Amazon BR (R$32,06) segue sem `usuario_id`, sem ação — não foi resolvida nesta sessão.

**3. Inbox Financeira desativada da UI — pedido explícito do usuário ("desative o inbox, isso só gera duplicidade")**: itens já resolvidos/explicados pelo usuário reapareciam na fila sem memória da decisão anterior, gerando frustração repetida. Resumo + tabela + botões aprovar/rejeitar escondidos (`display:none`) em `Sistema_Wallace_Lira_Completo.html`, seção 21 — **não deletado**: a sincronização Pluggy/Mercado Pago continua rodando por baixo (outras telas, como o dashboard do Mercado Pago, ainda leem `VARS.INBOX_FINANCEIRA`). O formulário "＋ Lançar" (lançamento manual, no mesmo card) continua visível e funcionando normal, intocado. Se o usuário quiser reverter, é só remover o `display:none`.

**Nota pra sessão futura**: a causa raiz de por que a Inbox "não funciona"/reabre item resolvido não foi investigada a fundo (usuário preferiu desativar direto em vez de esperar a investigação) — se for revisitar essa frente, começar por aí antes de tentar reativar a UI.

## ▶️ Continuação 15/08/2026 (bloco 16) — série de achados de UI/dado reportados ao vivo pelo usuário, todos corrigidos

Retomada do bloco 15 abaixo. Usuário foi reportando achados reais em sequência, olhando o painel ao vivo — cada um investigado com evidência (código e/ou SQL direto no Supabase) antes de corrigir, nenhum "consertado no escuro".

1. **Sugestão de LREI na Caixa Bens Duráveis removida** (`hydrate-qualidade.js`) — pedido explícito ("retire essa nova, não acrescenta nada"). Mantida a nota neutra de saldo negativo, só a sugestão de saldo positivo foi tirada.
2. **"🧠 Wealth Intelligence" faltando na Home** (`home-nav-grid`, `Sistema_Wallace_Lira_Completo.html`) — a aba foi criada no bloco 14 (Fase 2C do WWI) direto em `.master-tabs`, mas a grade de atalhos da Home nunca foi atualizada junto. Adicionado o botão que faltava.
3. **Barra `.master-tabs` torta no mobile** (`assets/css/styles.css`) — com 8 abas agora (era 5 quando o layout foi desenhado), `flex-wrap:wrap` com `flex-basis:40%` fixo fazia a última aba de linha ímpar esticar sozinha/cortar texto longo ("Wealth Intelligence", "+ Lançar"). Trocado por rolagem horizontal (`overflow-x:auto`, `flex:0 0 auto`), mesmo padrão Gmail/Android já usado no auto-hide da mesma barra.
4. **Botão "+ Lançar" só funcionava na aba Painel** (`ui-navegacao-basica.js`) — o formulário (`#formLancarTx`) só existe dentro do pane `#painel`; fora dele o form ficava com `display:block` mas escondido atrás de um `.master-pane` inativo (`display:none`), então o clique parecia não fazer nada. Corrigido: `abrirFormLancarTxRapido()` agora chama `showMaster('painel')` antes de mostrar o form.
5. **Investigação do "Comprometido no cartão" da Caixa Bens Duráveis** (R$1.087,29) — confirmado via SQL que a fórmula está certa, mas 3 das 6 transações somadas (R$627,99: TX000226/227/326) são equipamento do projeto solar/DDSU666, não bens duráveis pessoais. Cheguei a mover pra Caixa Manutenção, mas o usuário pediu pra reverter ("mantenha como eu montei") — revertido de volta pra Bens Duráveis via SQL. Fica registrado como achado válido, não como pendência a resolver de novo.
6. **Campo "Pendente acumulado" (R$1.544,11) removido** do card de Reembolsos (`Sistema_Wallace_Lira_Completo.html` + `hydrate-balanco.js`) — pedido do usuário ("não comunica nada"). Ficava travado numa dúvida de confirmação desde 09/08 (se já estava contido no Amount Due Employee da Wärtsilá) sem nunca resolver; decisão foi remover em vez de manter em aberto.
7. **Bug real nas abas LRW/LRV (Mastercard Black) — item de "limbo" na posição/data errada** (`hydrate-onda3-lrwlrv.js` + `app.js`): os 2 lançamentos pós-fechamento de fatura (TX000132/TX000154, literais fixos porque a view filtra por `ciclo_inicio_em`) entravam via `.unshift()`, sempre na posição 0 — apareciam como se fossem a compra mais recente do ciclo, mesmo sendo do dia 22-24/07 (antes de tudo mais). A view também não tinha nenhum `order=` explícito (diferente da irmã LRC_LIMBO, que já usa `order=data.asc`). Corrigido: `order=data.asc` adicionado na query, e o `unshift()` trocado por uma função que insere o item na posição cronológica correta em vez de sempre no topo.

Achado de investigação registrado, sem mudança de código: "Pendente acumulado" (item 6) segue sem confirmação sobre a origem real (viagens Recife/JP no Amount Due Employee da Wärtsilá) — como o campo foi removido da tela, essa dúvida deixa de ser visível, mas o valor histórico (R$1.544,11, VARS.faturaMPCorporativoPendente) continua existindo internamente, só não é mais mostrado.

## ▶️ Continuação 15/08/2026 (bloco 15) — bug crônico do Drive sincronizando `.git/` corrigido na raiz (não mais reativo)

Retomada do bloco 14 abaixo. Pedido do usuário depois de eu limpar mais um `desktop.ini` de rotina: "como resolver isso de vez?". Regra 6 do `ESTADO_ATUAL.md` já registrava que as limpezas anteriores (13-14/08, 15/08) eram sempre reativas e nunca eliminavam a causa raiz — o Google Drive para desktop no Windows não tem exclusão seletiva de subpasta dentro de uma pasta já sincronizada, então `.git/` (com milhares de arquivos pequenos trocando o tempo todo) sempre voltava a ser indexado, gerando `desktop.ini` dentro de `.git/objects/` e quebrando `git push`/`git pull` com `bad object refs/heads/.../desktop.ini`.

**Confirmação ao vivo do problema antes de corrigir**: logo após um `git pull` limpo, 2 `desktop.ini` novos já tinham aparecido dentro de `.git/objects/` — o Drive indexa em tempo real, não é um evento raro.

**Correção aplicada**: `.git` real movido pra fora da árvore sincronizada pelo Drive, pra `C:\Users\WLI015\.git-repos\Site.git` — mecanismo padrão do próprio Git (o mesmo usado por submodules/worktrees): o diretório `.git` da pasta do projeto virou um arquivo de 1 linha (`gitdir: C:/Users/WLI015/.git-repos/Site.git`), e o `core.worktree` no `.git` relocado aponta de volta pra `G:/My Drive/Livro Razão/Site`. Testado ao vivo: `git status`, `git log`, `git pull` (trouxe o commit `f3bbdab` que estava pendente) — tudo funcionando idêntico a antes, working tree limpa.

**Escopo da correção**: é local à máquina `WLI015`, não ao repositório (não é algo que vá pro GitHub). Se este repositório for aberto numa máquina nova sem essa pasta `.git-repos`, o Git vai reclamar que não é um repositório — não é bug, é reconfiguração de uma vez por máquina nova (mesma raciocínio de qualquer `git clone` novo). Documentado em `ESTADO_ATUAL.md`, regra 6 (marcada RESOLVIDO NA RAIZ, não mais mitigação).

## 🏁 Encerramento 15/08/2026 (bloco 14) — WWI: Fases 1, 2 e 3 concluídas, sistema entra em estabilização operacional

Retomada do bloco 13 abaixo. Sessão inteira dedicada ao **WWI (Wallace Wealth Intelligence)**, que já tinha virado a frente principal no fechamento do bloco 13. Arco completo: roadmap de 3 fases → execução das 3 → shadow mode → congelamento funcional por decisão do usuário. Detalhe técnico completo em `docs/decisions/WWI_ROADMAP_V1.md` (18 seções), `WWI_NARRATIVE_ENGINE_ANALISE.md`, `WWI_FASE2_PROPOSTA_ARQUITETURA.md`, `WWI_FASE3_LEVANTAMENTO_TECNICO.md`.

**Fase 1 — narrative engine unificado** (aprovada em etapas, checkpoint a checkpoint):
- `metodologia_versao` + `vw_wwi_comparativo_mensal` (M/M·T/T·A/A de score/patrimônio, `NULL` explícito sem histórico suficiente) — item 1 e 2, aprovados separadamente antes do resto.
- Análise pré-unificação exigida pelo usuário (`WWI_NARRATIVE_ENGINE_ANALISE.md`) antes de qualquer código: achou que o JS tinha 15 regras de narrativa contra 6 do Python — estratégia de 2 estágios proposta e aprovada.
- **Estágio A** (aditivo): 8 das 9 regras faltantes + 5 blocos estruturados portados pro Python, usando `pib_wallace_historico` da própria competência (descoberta chave — é escrita a cada boot do painel, não só no fechamento) + `caixas.teto_mensal` + `emprestimos_internos`. `capacidade_investimento` fica gap aceito (sem fonte SQL, decisão de não fabricar).
- Validação real (não só comparação de JSON) achou 2 rodadas de gap: contrato `{texto,valor}` errado (corrigido) e, numa auditoria mais funda contra os RENDERIZADORES reais do PDF, **3 bloqueantes de verdade** (`projetos[i].linhas`/`composicaoPatrimonio.linhas`/`liquidezAnalise.linhas` — causariam `TypeError` real, não só perda de texto). Corrigidos e validados por simulação dos acessos exatos dos renderizadores.
- **Estágio B liberado como SHADOW MODE, não substituição**: usuário foi explícito — motor JS continua funcionando normalmente, nunca é removido; quando a narrativa Python persistida é reaproveitada, o JS calcula a própria versão só pra comparar e loga divergência no console (`wwiCompararNarrativaShadow`).

**Fase 2 — Snapshot Patrimonial Completo** (autorizada ponta a ponta, sem checkpoint entre 2A/2B/2C):
- Proposta de arquitetura completa antes de qualquer código (`WWI_FASE2_PROPOSTA_ARQUITETURA.md`) — achado principal: os 12 campos "obrigatórios" já eram persistidos desde o Estágio A, nenhuma tabela nova necessária.
- **2A**: `vw_wwi_metricas_historico` (formato longo, 14 métricas, M/M·T/T·A/A com checagem de contiguidade de calendário — nunca compara meses não-consecutivos como M/M).
- **2B**: `vw_wwi_score_historico` (melhor/pior por metodologia, média móvel de 3, tendência só a partir do 4º ponto) + checagem de sanidade no job (avisa em >30% de campo ausente, nunca bloqueia).
- **2C**: aba permanente `#wwi` ("🧠 Wealth Intelligence") no painel — decisão de produto pausada 1x pra perguntar onde deveria morar (condição de parada do próprio usuário), decidiu aba permanente na navegação principal. 8 seções, consome 100% de dado persistido, zero dependência do PDF.

**Fase 3 — PDF como consumidor do WWI** (autorizada ponta a ponta):
- Levantamento técnico primeiro (`WWI_FASE3_LEVANTAMENTO_TECNICO.md`): achou que `indicadores` era SEMPRE recalculado ao vivo, mesmo com WWI persistido disponível — achado que só apareceu porque a Fase 1/Estágio A.1 já tinha mapeado esse detalhe.
- Migrado: `indicadores` (Wealth Score/subscores/16 campos) lê `dados_json` quando a competência está fechada, com guarda + fallback automático (nunca quebra o relatório). Migra em cascata Reembolsos Wärtsilä e o gauge do Score. 5 de 8 KPIs do Painel Executivo passam a preferir o campo persistido.
- Shadow mode novo pro comparativo (`wwiCompararComparativoShadow`) — não substitui o texto exibido, só compara e loga.
- **2 decisões de produto explicitamente adiadas pelo usuário** (não é pendência técnica): manter os 2 enquadramentos do comparativo em shadow mode até observar ≥1 ciclo real; NÃO portar Liquidez Imediata/Geração de Caixa pro Python agora ("71%→100% é meta técnica, não meta de negócio, prefiro estabilidade").
- Adoção final: **≈71% dos blocos de cálculo do PDF já consomem WWI** quando a competência está fechada.

**Encerramento formal, diretriz do usuário**: *"WWI entra oficialmente em modo de estabilização operacional."* Fases 1/2/3 concluídas, Shadow Mode em observação, Estágio B continua bloqueado. Não abrir fase nova, não criar métrica nova, não expandir escopo, não refatorar por refatorar — só agir se houver divergência real observada nos logs de shadow mode ou pedido explícito. Memória de agente salva (`project_wwi_status_1508.md`) pra sessões futuras não reabrirem sem necessidade.

**Padrão operacional desta sessão, repetido várias vezes**: "8 agentes especializados em paralelo" pedido pelo usuário pra cada fase — na prática, quase todo o trabalho concentrava em 1-2 arquivos por vez (`gerar-analise-financeira.js`, `index.html`, `wwi_gerar_relatorio_mensal.py`), então paralelismo de verdade ali corromperia por escrita concorrente. Executado sequencial e diretamente em todos os casos, com nota explícita disso em cada entrega. Nenhum checkpoint pedido pelo usuário foi pulado quando ele pediu explicitamente pra parar em algo (ex: escolha de onde a aba WWI deveria morar) — só os checkpoints intermediários que ele mesmo dispensou ("não retorne até a fase inteira estar concluída") foram de fato pulados.

**Commits desta sessão** (todos publicados em `origin/main`, verificar `git log` pra hashes exatos): metodologia_versao + vw_wwi_comparativo_mensal; correção Estágio A (8 regras + 5 blocos); correção de gaps funcionais (buckets + contrato `{texto,valor}`); correção de contrato Estágio A.1 (3 bloqueantes reais); shadow mode da narrativa (Estágio B); proposta de arquitetura Fase 2; job sanity check (2B); aba Wealth Intelligence (2C); Fase 3 (3A+3B, indicadores + shadow mode do comparativo); levantamento técnico Fase 3; encerramento/registro de cada fase nos docs. Views SQL (`vw_wwi_metricas_historico`, `vw_wwi_score_historico`, extensão de `vw_wwi_comparativo_mensal`) aplicadas direto via Supabase MCP, mesmo padrão já usado desde a Fase 1 — não versionadas como arquivo de migration local.

## ▶️ Continuação 15/08/2026 (bloco 13) — Inbox Financeira processada (55 itens), "prioridade 0" das melhorias de médio prazo da auditoria

Retomada do bloco 12 abaixo. Duas frentes:

**1. Inbox Financeira** (pedido do usuário "e as 41 pende[ncias]", volume real tinha crescido pra 211): processados 55 sem risco — 41 duplicatas de sincronização multi-conta (`wallace.termica@`/`wallace.servidor@` lendo a mesma conta Mercado Pago 2x, achado estrutural, vai continuar acontecendo a cada sync até `arquivar_inbox_historico()` aprender a dedupli­car entre conexões), 11 taxas de IOF residuais, 2 TEDs Wärtsilä já lançadas no ERP (`TX000220`/`TX000280`). Achado inicial de que isso contradizia a pendência de R$340 do ciclo 2026-07 foi descartado pelo próprio usuário — são reembolsos diferentes, coincidência de valor. Restam 144 Pluggy + 13 MP que precisam de revisão caso a caso (maior valor, sem correspondência óbvia em `transacoes`).

**2. "Resolva tudo com prioridade 0"** (melhorias de médio prazo da auditoria de 43 especialistas, seção 4 do relatório): resolvidos 8 dos ~16 itens, todos aplicados direto via Supabase MCP + commit de código:
- 12 tabelas financeiras ganharam trigger de `audit_log` (patrimonio, financiamentos, parcelas, emprestimos_internos, reembolso_wartsila_ciclo, reembolso_wartsila_recebimentos, reembolsos, cartoes, investimentos, contas_bancarias, metas, usuarios) — reaproveitando `fn_audit_log_generic()` já existente, mesma régua que fechou o incidente de `historico_relatorios`.
- `pib_wallace_historico` restrita a login Firebase válido (era SELECT público) — confirmado antes que o único consumidor real (`Sistema_Wallace_Lira_Completo.html`) já manda o JWT real quando logado.
- `concurrency:` adicionada em 6 workflows (sincronizar_pluggy, mercadopago_sync, atualizar_geracao_saj, atualizar_cotacoes_acoes, backup_externo, wwi_regenerar_relatorio_mensal, executar_tudo) — `cancel-in-progress:false`, nunca mais 2 disparos sobrepostos duplicando sincronização.
- Falha parcial de sync (Mercado Pago/Pluggy) corrigida — antes, se 1 de N contas/bancos falhasse, o job inteiro terminava "sucesso" no heartbeat porque só travava quando TODAS falhavam. Agora reflete a falha parcial real no painel de Saúde Operacional, sem travar a resiliência original (as contas que deram certo continuam sincronizando).
- Painel de Saúde Operacional ganhou 2 entradas que já gravavam heartbeat mas nunca apareciam (`backup_externo`, `wwi_relatorio_mensal`).
- Chart.js ganhou SRI (hash sha512 real via `api.cdnjs.com`, não inventado) nos 2 arquivos que carregam via CDN — testado ao vivo no navegador, `window.Chart` continua carregando normal.
- Ambiguidade de redação corrigida no manual: "4 caixas em V1" parecia pendência em aberto, na verdade é exceção formal já aceita (`docs/decisions/EXCECOES_FORMAIS_DESLIGAMENTO_V1.md`).

**Não resolvido, motivo registrado em cada caso** (não são pendências esquecidas, são decisões conscientes de não mexer sem mais informação/decisão do usuário): segredo do webhook Pluggy (`PLUGGY_WEBHOOK_SECRET` hardcoded na Edge Function — preciso que o usuário configure o secret no Supabase antes, senão a troca de código quebra o webhook em produção sem eu conseguir testar); pergunta arquitetural sobre `src/services/*.js` (FinanceEngine/Comparator/CycleEngine parcialmente morto); `graficos-cenarios-lazy.js` carregado sempre apesar do nome "lazy" (precisa investigar o gatilho de carregamento antes de mexer, risco de quebrar renderização); lint dos ~91 módulos `hydrate-*`; 3 itens de UX (scroll position ao trocar aba, confirmação no botão Rejeitar da Inbox, atalho pro formulário de lançamento); arquivamento do `PASSAGEM_DE_TURNO.md` por competência.

Relatório atualizado com status completo em `docs/decisions/AUDITORIA_MULTIDISCIPLINAR_15082026.md`.

## ▶️ Continuação 15/08/2026 (bloco 12) — leitura solar bloqueada pelo trigger: causa raiz era data errada (não o teto), corrigido com evidência fotográfica

Retomada do bloco 11 abaixo (mesmo dia). Pendência deixada na fila desde antes do bloco 10 (pergunta encaminhada de uma sessão de Claude Chat): `INSERT` de leitura solar manual (casa "mãe", 14/08, `leitura_03=90`/`leitura_103=527`, 2 fotos reais) bloqueado por `validar_plausibilidade_leitura_solar()` — delta contra a linha de "13/08" (81/477) dava 50 kWh/dia no código 103, acima do teto de 40.

**Investigação (antes de pedir mais dado ao usuário)**: consultado o histórico completo de `energia_solar_leituras` da casa "mãe" e o campo `evidencia` de cada linha. Achado: a linha de 07/08 já documentava um incidente idêntico antes (leitura de 08/08 com salto de 51 kWh/dia, descartada por "provável erro de leitura/digitação") — mesma classe de problema podia estar se repetindo. Recalculando o delta usando a linha de 11/08 (75/444, evidência menciona foto) como referência em vez da de 13/08 (81/477, evidência só menciona "via chat", sem foto), o salto sumia (dentro do teto) — sinal de que a linha de 13/08, não a de 14/08, era a suspeita.

**Confirmação do usuário**: enviou as 2 fotos originais da leitura de 13/08 (03=00081, 103=00477, batendo exato com o banco) e disse que essa leitura foi tirada no dia **12/08 às 21:49**, não no dia 13 — "não fiz leitura dia 13". Comparando `created_at` da linha (`2026-08-13 03:14 UTC` = `13/08 00:14` em Brasília) com o horário real da foto (12/08 21:49), confirma o bug: quem gravou a linha (Claude Chat) usou "a data de hoje" no momento de gravar, já depois da virada da meia-noite em Brasília, em vez da data real que o usuário informou.

**Correção aplicada** (SQL direto no Supabase, MCP): `UPDATE` na linha existente — `data` `2026-08-13`→`2026-08-12`, evidência reescrita documentando a correção e a causa raiz. Depois, `INSERT` da leitura de 14/08 (90/527) — passou no trigger sem nenhum bloqueio (delta real 12/08→14/08: 2 dias, 4,5 kWh/dia código 03, 25,0 kWh/dia código 103, dentro do teto).

**Resposta às 3 perguntas originais do Claude Chat**: não mexer no teto de 40 kWh/dia (já pegou 2 erros reais até hoje, incluindo este) e não criar mecanismo de override — nenhuma das duas coisas era o problema real, o teto estava certo, o dado é que estava errado.

**Regra nova registrada** (seção "Regras permanentes" do `ESTADO_ATUAL.md`, item 13): leitura manual de `energia_solar_leituras` sempre usa a data/hora que o usuário informa que tirou a foto, nunca "a data de hoje" no momento de gravar — já são 2 incidentes da mesma classe. Se o trigger bloquear de novo, a 1ª hipótese a checar é data errada na leitura anterior, não "o teto está errado".

**Fechamento estrutural, pedido explícito do usuário** ("como corrigir esse problema de UTC, hoje o medidor vai entrar em funcionamento, mas é bom resolver"): criada RPC `registrar_leitura_solar_manual()` — não existia nenhuma função dedicada pra leitura manual antes (entrava via `INSERT` direto, sem validação de fuso). A RPC exige o instante real da leitura como `timestamptz` e deriva a data civil de Brasília via `AT TIME ZONE 'America/Sao_Paulo'` (testado isoladamente: 12/08 21:49 → `2026-08-12`, correto). `GRANT` restrito a `authenticated`/`service_role`, mesmo padrão de `lancar_transacao_manual`. Documentação atualizada e **sincronizada nos 2 documentos do Google Drive** (`CUSTOM_INSTRUCTIONS_SISTEMA_WALLACE.md`/`MANUAL_OPERACIONAL_AGENTES.md`, sobrescritos, não cópias novas) — Claude Chat (quem realmente grava essas leituras) precisa usar essa RPC a partir de agora.

## ▶️ Continuação 15/08/2026 (bloco 11) — usuário deu autonomia total ("faça o melhor"), 17 achados adicionais da auditoria resolvidos (5 ALTA + 12 quick wins)

Retomada direta do bloco 10 abaixo (mesma auditoria de 43 especialistas). Depois de corrigir 2 falsos-positivos apontados pelo usuário (cadência do job SAJ é intencional — 10 em 10 minutos, não bug; "duplicata" Anthropic R$52,95 não é duplicata) e resolver os 2 críticos de segurança + a constraint de cartão (registrados no bloco 10), o usuário disse "vai resolvendo 1 por 1" e depois "faça o melhor, você tem autonomia" — resolvidos mais 17 achados nesta sessão, todos código/config (nenhuma correção de dado financeiro real, nenhum reprocessamento retroativo de relatório já publicado).

**5 achados ALTA (commit `c4906df`)**:
1. XSS parcial — escape aplicado nos 3 arquivos que faltavam (`render-livros-variaveis.js`, `render-parcelamentos.js`, `hydrate-onda9-livros-fixos.js`).
2. Constraint `chk_cartao_nao_afeta_saldo_real` em `transacoes` — impede fisicamente repetir o bug (já visto 6x no passado) de compra de cartão reduzindo saldo real. Testada ao vivo (BEGIN/ROLLBACK) antes de aplicar.
3. CI novo (`testes_unitarios.yml`) rodando os 3 testes unitários que nunca eram executados automaticamente.
4. Wealth Score do Python: implementados `construcaoPatrimonial` (via `pib_wallace_historico`, mesma fonte do painel) e `organizacaoFinanceira` (proxy adaptado, sem DOM) — antes sempre `None`, 35% do peso do score. Reprocessamento retroativo do histórico NÃO feito, decisão fica pro usuário.
5. Guard `patrimonioLiquido > 0` explícito (era truthy) em `protecaoPatrimonial`/`investimentos`, nos dois motores — corrige um cenário onde patrimônio líquido negativo dava nota 100 (proteção máxima) no pior caso possível.

**12 quick wins** (mesma sessão, ainda não commitados no momento em que este bloco foi escrito): `robots.txt` + `<meta robots noindex>` (3 páginas), `scope="col"` em 48 `<th>`, contraste do placeholder de login, `aria-pressed`/`aria-label` no botão de esconder valores, `role="dialog"`+Escape+`aria-live` no modal de PIN, comentário desatualizado da Inbox Financeira corrigido, `teste_cron.yml` removido (workflow de diagnóstico esquecido, `actions:write` em produção), favicon realinhado ao `--accent` real do CSS (SVG + 3 PNGs regenerados com Pillow), `apple-touch-icon` adicionado em `solar-compartilhado.html`/`404.html`, guard da RPC `atualizar_mercadopago_eventos` padronizado pra allowlist (era blocklist, inconsistente com a RPC irmã), `ORDER BY` determinístico nos 4 `DISTINCT ON` de `atualizar_pluggy_contas` (testado isoladamente, sem tocar tabela real), retry/backoff no `_request()` do Pluggy (mesmo padrão já usado no Mercado Pago).

Relatório de status completo, item por item, em `docs/decisions/AUDITORIA_MULTIDISCIPLINAR_15082026.md` (tabela no topo). Restam só as "Melhorias de médio prazo" da seção 4 (nenhuma urgente) e a decisão sobre reprocessar ou não o histórico do WWI com a metodologia nova do Wealth Score.

## ▶️ Continuação 15/08/2026 (bloco 10) — auditoria multidisciplinar de 43 especialistas seniores, 207 achados, 2 achados críticos de segurança reais

Retomada do bloco 9 abaixo. Pedido explícito do usuário: montar uma "equipe de agentes especialistas, todos senior... trabalhando em equipe multidisciplinar" cobrindo 11 departamentos (Liderança/Estratégia, UX, Front-end, Back-end, Infra/Cloud, Segurança, Qualidade, Marketing, Dados/Finanças, IA, Conteúdo) somando 43 papéis nomeados, "escopo livre", para analisar, corrigir e melhorar o site.

**Decisão de execução**: rodado como `Workflow` de 43 agentes em paralelo (1 por papel) + 1 agente de síntese — cada especialista com contexto do projeto (é sistema financeiro pessoal/familiar, não produto comercial) e instrução explícita de fase SOMENTE LEITURA (nenhuma edição de arquivo, nenhum commit). Justificativa: dado o volume de logica financeira sensivel e as regras já documentadas no manual (nunca commitar sem avisar, nunca aplicar mudança de dado sem confirmar, cuidado com V1×V2), aplicar 43 conjuntos de mudanças as cegas em paralelo seria o tipo de risco que o próprio manual pede pra evitar — a auditoria virou "achar e priorizar", não "corrigir direto"; a decisão de que corrigir fica com o usuário.

**Resultado**: 207 achados brutos, consolidados no relatório final em `docs/decisions/AUDITORIA_MULTIDISCIPLINAR_15082026.md`. Achados mais importantes:
- **2 CRÍTICOS de segurança real, confirmados ao vivo via Supabase** (DBA + Cybersecurity + Pentester, não é hipótese): a RPC `wwi_upsert_relatorio_mensal` é gravável por qualquer `anon`/`authenticated` sem checagem de autenticação (todas as outras 8 RPCs de escrita do sistema têm essa checagem, essa não tem); e a tabela `wallace_dados` (V1 legado) tem policy de SELECT pública — qualquer pessoa com a chave pública do site lê o blob financeiro completo da família sem login.
- **ALTA**: motor Python do Wealth Score (`wwi_gerar_relatorio_mensal.py`) está estruturalmente incompleto — falta implementar 2 dos 7 sub-scores (`organizacaoFinanceira`, `construcaoPatrimonial`, 35% do peso), então diverge do motor JS por design, não só por bug pontual (achado por 4 papéis diferentes de forma independente).
- **ALTA**: XSS corrigido em 14/08 cobriu só parte dos arquivos que interpolam texto de usuário em `innerHTML` sem escape — ainda falta em `render-livros-variaveis.js`, `render-parcelamentos.js`, `hydrate-onda9-livros-fixos.js`.
- **ALTA**: regra "compra de cartão nunca reduz saldo real" (já violada 6x historicamente antes de virar regra documentada) não tem nenhuma constraint de banco que impeça reincidência.
- **ALTA**: job de sondagem do medidor solar SAJ disparando ~144x/dia em vez de 2x/dia (achado antigo de 08/08, nunca resolvido, cadência configurada no cron-job.org externo, não no YAML).
- **ALTA**: transação real duplicada não capturada pelo filtro da Inbox (Anthropic R$52,95 lançada 2x).
- **ALTA**: zero CI — os 3 arquivos de teste unitário existentes (`tests/unit/*.test.js`) nunca rodam automaticamente em push/PR.
- Lista completa de quick wins (acessibilidade, SEO, favicon, robots.txt), melhorias de médio prazo por tema, e uma lista de 11 itens que **exigem validação explícita do usuário** antes de qualquer implementação (a maioria toca dado financeiro real ou contradiz decisão já documentada) — tudo no documento.

**Nota**: a síntese do workflow citou como achado "correção do Wealth Score pendente de commit" — isso é falso-positivo temporal: a auditoria rodou em paralelo com o commit `7d80c59` (bloco 9), então o agente de síntese não sabia que já tinha sido resolvido no meio do caminho. Corrigido no documento final.

**Fechamento desta rodada**: nenhuma correção foi aplicada — é auditoria pura, por decisão de segurança (ver acima). Documentos atualizados (`ESTADO_ATUAL.md` seções 1.-2/1.-1, este arquivo, `docs/decisions/AUDITORIA_MULTIDISCIPLINAR_15082026.md` novo). Próximo passo real: usuário revisar o relatório e decidir prioridade, começando pelos 2 achados críticos de segurança.

## ▶️ Continuação 15/08/2026 (bloco 9) — correção de metodologia no Wealth Score do WWI: `protecaoPatrimonial` duplicava `endividamento`

Retomada do bloco 8 abaixo. Sessão focada, achado único: auditoria de metodologia (analista financeiro sênior) no Wealth Score do WWI encontrou que o sub-score `protecaoPatrimonial` usava a fórmula EXATA de `endividamento` — `100 - (passivosTotal/ativosTotal*100)/50*100` nos dois, mesmo limiar de 50% de dívida sobre ativo total. Isso fazia o mesmo fato (nível de alavancagem) contar 2x na média ponderada dos 7 sub-scores, inflando o peso real da dívida pra 30% (15%+15%) sem que essa fosse a intenção documentada dos pesos (seção 4 de `docs/decisions/WWI_RELATORIO_EXECUTIVO_INTELIGENCIA.md`).

**Correção**: `protecaoPatrimonial` agora mede um ângulo genuinamente diferente — debt-to-equity, passivos sobre **patrimônio líquido** (não ativo total): `100 - (passivosTotal/patrimonioLiquido*100)`, nota zero quando a dívida iguala o patrimônio líquido inteiro. Mais sensível quando o ativo é majoritariamente ilíquido (caso deste usuário, imóvel pesa muito no ativo total) — "quanto sobra de capital próprio de verdade depois de honrar as dívidas" é uma pergunta diferente de "quanto do ativo bruto é dívida". `endividamento` continua medindo alavancagem bruta (passivos/ativoTotal), sem mudança. Nenhum dado novo precisou ser coletado — `passivosTotal` e `patrimonioLiquido` já eram calculados nos dois motores, só não eram cruzados dessa forma antes.

Corrigido em `src/relatorio/gerar-analise-financeira.js` (`calcularIndicadoresEScores`, botão manual/JS) **e** em `scripts/sync/wwi_gerar_relatorio_mensal.py` (job mensal/Python) — a paridade entre os dois motores já é uma régua conhecida deste projeto (bloco 8, item do sub-score "investimentos" tinha o mesmo tipo de divergência JS×Python). Documentado em `docs/decisions/WWI_RELATORIO_EXECUTIVO_INTELIGENCIA.md`, seção 9.1 nova.

**Pendente de fechamento**: mudança ainda não commitada — avisar o usuário e commitar/pushar (sem migration, é lógica pura, entra em vigor no próximo relatório WWI gerado). Nenhuma regra de negócio nova nem domínio V2 novo — não exigiu atualização dos documentos do Google Drive (Custom Instructions / Manual).

## ▶️ Continuação 14/08/2026 (bloco 8) — auditoria completa do site (1 agente por aba), 8 bugs reais corrigidos, Inbox Financeira redesenhada, favicon mobile e crédito solar corrigidos

Retomada do bloco 7 abaixo (mesmo dia). Sessão longa, várias frentes encadeadas.

**1. Procedimento de baixa da fatura do cartão — decisão fechada.** Pendência deixada aberta no bloco 7: quando a fatura vence e é paga de verdade, `UPDATE` na MESMA linha de `transacoes` (`afeta_saldo_real` false→true), nunca uma segunda transação. Documentado em `docs/decisions/GENERALIZACAO_COMPROMETIDO_CAIXAS_TEMATICAS.md` e seção 1.3.5 do manual.

**2. Medidor DDSU666 — Fase 1 (sondagem) preparada**, véspera da instalação física (15/08, Casa da Mãe). `scripts/sync/sondar_medidor_saj.py` + workflow `sondar_medidor_saj.yml`, só leitura, sem mudar o robô de produção. Commit `147a5eb`.

**3. Favicon desatualizado no mobile.** Usuário reportou "no mobile está o antigo ainda". Achado real: `favicon-32.png`/`favicon-192.png`/`apple-touch-icon.png` eram de 05/08 (design antigo), enquanto `favicon.svg` já tinha sido trocado pro gradiente azul/roxo em 12/08 — ninguém regenerou os PNGs junto, e Chrome Android usa PNG, não SVG. Regenerados com a mesma geometria exata do SVG (Pillow, sem dependência de rasterizador externo).

**4. Crédito solar dessincronizado entre painel privado e link de compartilhamento.** Usuário reportou números errados no `solar-compartilhado.html` ("meu e da minha irmã"). 1 agente investigou: causa raiz era o Fluxo 2 ("crédito em formação") usando só o saldo cru da última leitura manual, sem projetar os dias já passados desde então — o painel privado projeta até hoje somando geração real do robô SAJ e descontando consumo estimado. Reimplementada a mesma fórmula (bit a bit) em `solar-compartilhado.html`; validado contra a RPC real (creditoWallace bateu exatamente com o painel: R$82,75). Corrigido de quebra um desalinhamento de fuso (Brasília -3h). Commit `5d22216`.

**5. Auditoria completa do site — 6 agentes, 1 por aba, motivados pelo achado do item 4.** Hipótese: se uma superfície pública duplicava uma fórmula do painel de forma dessincronizada, pode haver bugs da mesma classe em outros lugares. Resultado, por aba:
- **Painel Executivo**: sem bug — já converge tudo pra `REG.operacional` como fonte única. Achado periférico (data hardcoded "25/06-24/07" na aba Cenários) virou tarefa em background separada (`task_f7eec61f`, rodada pelo usuário numa sessão paralela).
- **Gráficos/Cenários**: sem bug novo — a correção do item 4 já cobriu tudo (crédito líquido, projeção, Fluxo 1/2, metas), nenhuma outra fórmula duplicada no repo.
- **Balanço V2**: 2 achados reais. `passivoFinanciamentoCasa` travado desde a criação do arquivo em R$61.326,91 (real V2: R$61.081,39, diferença R$245,52) — corrigido. RPC `rpc_dashboard_resumo` filtrava por `data_snapshot` máxima GLOBAL em vez de por item, zerando 12 dos 13 itens de patrimônio (Patrimônio Líquido caía pra R$100.000 em vez de ~R$468 mil) — o painel tinha trava de segurança que impedia isso de contaminar o total principal, mas o selo "✓ confirmado: R$100.000,00" aparecia mesmo assim, de forma incondicional. Corrigidos os dois: migration no filtro da RPC (`DISTINCT ON (subtipo)`, mesmo padrão de `vw_patrimonio_v2`) e o selo agora respeita a mesma trava de tolerância.
- **Emagrecimento**: aba dedicada não mostrava "(−) Comprometido no cartão / (=) Disponível Real" que o card-resumo do dashboard já mostra pra mesma caixa — quem olhasse só a aba via "R$278,89 disponível" quando o disponível real é R$0. Corrigido reaproveitando `getComprometidoPorCaixaV2()`.
- **Livros Razão** (todas as sub-abas): 2 achados. Contador "(N)" dos botões ficava preso no valor V1 desde o boot (race condition — Onda 3 não notificava o módulo de contagem ao terminar, diferente dos módulos irmãos) — corrigido. Rodapé de soma das caixas temáticas bate matematicamente com "Disponível Real", não "Tem na Caixa" (mistura dinheiro real com compra de cartão não paga) — a conta está certa, só faltava rótulo; adicionada nota explícita quando aplicável.
- **Relatório WWI**: 3 achados. Patrimônio Líquido divergia R$2.630,26 entre o motor JS (botão) e o Python (job mensal) — causa raiz era a Caixa Lance, incluída no Python mas excluída no JS (por design, correto pro Painel/Balanço, mas não decidido pro WWI). `metas.valor_atual` é tabela órfã, usada só pelo Python, defasada (R$120.314,26 vs. real R$117.839,63). `historico_relatorios` com 0 linhas apesar do heartbeat do job mensal dizer sucesso hoje — sem trigger de auditoria pra rastrear o que aconteceu.

**6. Decisões do usuário sobre os achados acima, todas resolvidas na mesma sessão:**
- Caixa Lance **entra** no Patrimônio Líquido do WWI — corrigido só no cálculo do WWI (JS), sem tocar `recalcularPatrimonio()` usado pelo Painel/Balanço. Validado: os dois motores agora batem exatamente em R$471.458,31. De quebra, achou e resolveu (autorizado) uma 2ª nuance: consórcio da casa pago também precisava entrar no numerador do sub-score "investimentos" — decidido que sim (mesmo tratamento do Balanço Patrimonial do painel), sub-score bate 64,02 nos dois lados agora.
- `metas.valor_atual` — Python parou de depender da tabela órfã, calcula ao vivo (mesma fórmula do painel). Achado extra: a RPC do Balanço também dependia de `metas` pra um aviso morto (painel de auditoria V1×V2 aposentado em 09/08) — bloco removido.
- `historico_relatorios` vazia — **1 agente sênior investigou com rigor**: não era "sucesso sem gravação real" (a gravação aconteceu, confirmada em log de rede). Era um `DELETE` real, corretamente filtrado por competência (não foi "apagar tudo por engano"), sem nenhum rastro de quem/quando porque a tabela nunca teve trigger de auditoria e os logs do Postgres só capturam DDL. Trigger de auditoria criado (mesma régua de `transacoes`/`caixas`/`indicadores`). Linha de julho recalculada do zero via SQL (não copiada de documento) e batendo exatamente com os números já validados antes — recuperada. Autor do `DELETE` original continua desconhecido, sem como recuperar essa informação.
- Constantes hardcoded do "Déficit Zero" (Cenários) — usuário pediu pra tirar TODO hardcode de constante financeira: migradas pra `parametros_gerais.taxasHoraFolhaPontoWartsila` (mesma linha do Estimador de Salário), JS lê de lá com fallback. Regra nova documentada no manual (seção 1.4): nenhuma constante financeira nova deve nascer hardcoded se já existe (ou faz sentido existir) um lugar em `parametros_gerais`/`indicadores`.

**7. Inbox Financeira redesenhada — 1 especialista sênior, pedido explícito do usuário** ("aquilo foi pensado em mostrar apenas transações esquecidas... quero alguém com espertise que faça os filtros funcionarem perfeitamente"). Causa raiz real: o filtro de ciclo/dedup já existia desde 09-12/08, mas só escondia da tela — nunca gravava a decisão no banco, então os itens ficavam "pendente" pra sempre, só invisíveis (96% dos `mercadopago_eventos` pendentes eram de ciclos já fechados, o mais antigo de fevereiro). Achado bônus: um bug de formato de chave (`pluggy_triagem.id_externo`) fazia 361 rejeições antigas nunca serem reconhecidas pelo app. Corrigido: chaves normalizadas, nova função `arquivar_inbox_historico()` (arquiva ciclo passado + rejeita duplicata já lançada) conectada direto nos robôs de sincronização — roda sozinha daqui pra frente. Volume real caiu de ~557 pra 50. Dos 50, o usuário revisou os principais e confirmou (nenhum era esquecimento real): MEDINTECH/GUIMARAES FARIA já pagos via boletos automáticos; RAIA DROGASIL R$308,22 já lançada dividida em 2 transações (Emagrecimento + Variável); ANTHROPIC R$116,52/R$110,00 é a mesma compra já lançada como `TX000274`, capturada 2x pelo Pluggy/MP. Marcados rejeitados com evidência cruzada. Sobram 41 itens de ruído de baixo valor, não urgente. Documentado em `docs/decisions/INBOX_FINANCEIRA_REDESIGN_FILTROS.md`.

**Fechamento**: 1 commit grande (`80c96f9`) com todo o código desta rodada de auditoria, mais o commit do item 4 (`5d22216`) e do item 1-3 (`35e2069`), todos já publicados (push, com merge do bump automático de `__V`). Migrations aplicadas direto em produção nesta sessão (RPC do Balanço, trigger de `historico_relatorios`, redesenho da Inbox) — não passam por commit git. Achado de infraestrutura recorrente: o Google Drive voltou a sincronizar `.git/refs/` (mesmo bug de 13-14/08 já "resolvido" antes), quebrando o push com `bad object refs/heads/claude/desktop.ini` — limpo de novo (~208 arquivos), mas é sintoma de que a causa raiz (o Drive ainda enxerga `.git/` como pasta sincronizável) não foi eliminada de vez, só mitigada a cada vez que aparece.

## ▶️ Continuação 14/08/2026 (bloco 7) — WWI: saga de fidelidade visual até aprovação + PGV/R$1,32 + Inbox em massa + generalização de Comprometido (9 transações corrigidas)

Retomada direta do bloco 6 abaixo (mesmo dia, WWI recém-criado). Sessão longa, 4 frentes principais.

**1. WWI — várias reescritas até bater com o artefato aprovado.** O relatório de fechamento (criado no bloco 6) foi reprovado várias vezes seguidas até ficar idêntico ao "Tactical Wealth Report" que o usuário tinha aprovado antes numa conversa separada. Sequência real: (a) jsPDF desenhado à mão — reprovado, tipografia nunca fica igual a HTML/CSS real; (b) html2canvas reconstruindo o CSS **de memória** — ainda reprovado, com erros reais (fonte invertida, cor aproximada); (c) baixado o código-fonte REAL do artefato (`curl` no frame do Claude, não reconstruído) — CSS reescrito verbatim, resolveu tipografia/cor; (d) 3 bugs de renderização do html2canvas achados e corrigidos (pseudo-elemento `::after`+flex virando linha por cima de título, "%" faltando num KPI, qualidade de fonte); (e) estrutura errada — usuário: "tem muito mais abas, porém sem o detalhamento que o que aceitei possui" — 20 seções genéricas viraram as 11 curadas exatas do artefato; (f) **mudança de arquitetura**: abandonado html2canvas+jsPDF, botão agora abre a página HTML real numa aba nova (`window.open`+`document.write`), sem nenhuma rasterização — usuário perguntou direto "por que não gera um HTML idêntico ao do link?"; isso também resolveu de quebra um bug real (`window.print()` automático sendo **bloqueante**, travando a aba principal inteira, achado pelo usuário: "os dados voltaram a demorar carregar"); (g) qualidade "premium" dos KPIs — faltavam legenda (`.kpi-sub`) e selo semafórico (`.chip`) em cada card, e os 8 KPIs não eram os mesmos 8 do artefato (3 eram aproximações minhas, trocadas pelos exatos, incluindo "Liquidez Imediata" calculada como soma de 3 valores já coletados); (h) texto do CFO duplicado (resumo condensado + pontos por extenso repetindo o mesmo fato 2x) + números com ponto americano ("95.4%" em vez de "95,4%") + "Wärtsilä" saindo minúsculo — todos corrigidos; (i) favicon com bug de aspas duplas cortando o `href` no meio, depois trocado pro favicon padrão do site (pedido do usuário, não um ícone novo inventado); (j) **último achado, mesma sessão**: tabela de Análise de Patrimônio sem negrito nos totais/vermelho nas deduções/coluna "% do Ativo Bruto" que o artefato tem — corrigido com um helper único (`_wwiRelLinhaTabela`) que decide estilo só pelo texto do label já coletado (nunca inventa dado), aplicado em toda tabela do relatório; achado e corrigido um bug real no meio dessa correção (regex não reconhecia "en dash" –, só hífen normal, deixando as linhas de dedução sem vermelho).

**2. PIX Geral Vanessa (CC-103) — R$1,32 de diferença real.** Usuário reportou card desincronizado do app real. Reconciliação transação por transação (17 no sistema × extrato completo do usuário) confirmou: sistema matematicamente correto pro que conhecia, diferença é rendimento de cofrinho nunca capturado — mesmo padrão já usado em outras 11 caixas num lote de calibração de 13/08, só que PGV tinha ficado de fora. Lançado `TX000321` (R$1,32), saldo fechou em R$283,05.

**3. Inbox Financeira — triagem em massa (regra 6 do manual).** Achado real: 479 `mercadopago_eventos` + 556 `pluggy_transacoes` pendentes, muito acima do normal. Processado com cautela: 111+254+107 = 472 rejeitados por duplicata confirmada (cascata Wärtsilä já processada, mecânica interna do cofrinho, ou valor+descrição correlata — exigida correlação de texto depois de achar 1 falso-positivo real de coincidência de valor). 563 continuam pendentes por falta de sinal seguro pra classificar sem risco (descrição em branco ou só valor batendo sem descrição).

**4. Generalização de "Comprometido × Disponível Real" — pergunta de arquitetura do Claude Chat, decisão do usuário, 9 transações reais corrigidas.** Claude Chat trouxe uma pergunta (achado dele: compra de R$104,30 no cartão, lançada na Caixa Bens Duráveis, sem efeito visual no saldo). Investigado: o conceito só existia pra Caixa Variável, não por decisão documentada — nunca tinha sido generalizado. Usuário decidiu generalizar pras 6 caixas temáticas com compra de cartão (Churrasco, Bens Duráveis, Manutenção, Eventos, Saúde Família, Emagrecimento) — implementado `getComprometidoPorCaixaV2()` + novo módulo de hydrate. Na auditoria pra implementar isso, achadas **6 transações reais mal classificadas** (`cartao_id` preenchido mas `afeta_saldo_real=true`, deveria ser sempre `false`) — corrigidas, mas isso quase **dobrou** 3 saldos. Usuário percebeu na hora ("o valor não pode dobrar") e apontou a causa: **3 lançamentos de calibração duplicavam** dinheiro já contado (criados numa época em que a compra de cartão ainda subtraía do saldo). Removidos. Saldos finais corretos: Bens Duráveis R$583,99, Churrasco R$358,87, Emagrecimento R$278,89. **Regra nova documentada** na seção 1.3.5 do manual (propagada pra qualquer Claude): compra de cartão nunca reduz saldo real, em nenhuma caixa.

Commits desta sessão (WWI, cronológico): favicon inicial → estrutura 11 seções → texto/formatação/favicon padrão → 8 KPIs exatos → legenda+selo → tabela de Patrimônio com %/negrito/vermelho. Dado financeiro (PGV, Inbox, Comprometido) via SQL direto no Supabase, não passa por commit git — cada operação confirmada com o usuário antes (dry-run implícito via pergunta, nunca bulk-update sem checar risco).

## ▶️ Continuação 14/08/2026 (bloco 6) — WWI (Wallace Wealth Intelligence), módulo permanente de relatório executivo vivo

Retomada do bloco 5 abaixo (mesmo dia). Pedido do usuário, depois de já ter recebido um "Tactical Wealth Report" avulso (artefato HTML gerado numa conversa anterior, estilo BTG/XP Private com dados reais do Supabase): "não crie apenas um relatório, crie um módulo permanente... o usuário NÃO quer PDF estático, quer um relatório vivo, regenerado automaticamente a cada mês". Antes de implementar, entrei em modo de planejamento (2 agentes Explore em paralelo mapeando a infraestrutura existente de PDF/snapshot/RPC/jobs, 1 agente Plan desenhando a arquitetura) e alinhei 4 decisões em aberto com o usuário via `AskUserQuestion` (todas na opção recomendada): competência = ciclo financeiro (25→24), não mês calendário; quem escreve o snapshot é um job mensal automatizado, não o clique do botão; motor de narrativa é regras determinísticas, sem LLM externo; PDF continua só download client-side. Um agente separado também escreveu o PRD completo do produto, entregue ao usuário no meio do fluxo de planejamento (pedido dele, "coloque outro agente pra me dar o pdr").

Implementação (Fase A, código): `coletar-dados-relatorio.js` ampliado de 6 pra 15 seções — cada título conferido contra o HTML real antes de adicionar (achado real: um agente de auditoria tinha dito que "Taxa de Poupança" não existia, estava errado, a seção existe aninhada dentro de outra, corrigido antes de descartar). Novo 3º extrator de DOM pro formato usado por Meta do Milhão/Passivos/Consórcio Casa Nova. `src/relatorio/gerar-analise-financeira.js` novo: Wealth Score (7 sub-scores ponderados, pesos redistribuídos quando falta dado — nunca trata ausência como zero) + 4 índices + narrativa por regras condicionais (nenhuma frase cita número que não veio do coletor) + comparação histórica com degradação graciosa. Botão Download reescrito pra buscar histórico, reutilizar narrativa já persistida ou gerar uma nova só pra exibir, e desenhar um PDF de 14 seções.

Implementação (Fase B, produção — pedido do usuário "prossiga"): migration aplicada (tabela `historico_relatorios` + RPC `wwi_upsert_relatorio_mensal`), testada com dado fake antes de remover (confirmado: update sobrescreve números, preserva narrativa original). Script Python do job mensal (`scripts/sync/wwi_gerar_relatorio_mensal.py`) calcula os mesmos indicadores via SQL direto nas views V2 (sem navegador, não dá pra fazer scraping de DOM) — cobertura parcial documentada onde não há fonte SQL confiável ainda (ex. "Total operacional"), testado isoladamente (cálculo de competência com 3 datas, geração de narrativa com dado fake). Workflow do GitHub Actions criado no mesmo padrão dos outros jobs (sem `schedule` nativo, disparo externo via cron-job.org). Commitado e pushado (`38d00c4`/`247a107`), avisando antes.

**Pendência real, só o usuário resolve**: agendamento mensal (dia 25) no painel do cron-job.org — conta externa, agente não tem acesso, mesma classe de limitação já documentada pra GitHub Secrets em sessões anteriores. **Não testado ao vivo** (mesma ressalva do relatório de fechamento original): login real não é possível neste ambiente — só verificação de sintaxe (`new Function()` no navegador, já que Node não está disponível neste ambiente) e testes isolados das funções puras.

## ▶️ Continuação 14/08/2026 (bloco 5) — F5 no painel nunca voltava pra mesma aba, feature já existia desde 10-11/08 mas nunca funcionava de verdade

Retomada do bloco 4 abaixo (mesmo dia). Usuário reportou: "quando recarrego a página sempre volta para o dashboard, eu quero que fique na mesma página". Achado real: o mecanismo de restauração de aba (`sessionStorage.wallaceAbaAtual` + `wallaceEhReload()`) checava se o próprio arquivo do painel tinha sido recarregado — mas esse arquivo só existe dentro de um `<iframe>` recriado do zero pelo `index.html` a cada login, nunca sofre um "reload" de verdade na visão dele. A checagem sempre dava falso e ainda apagava a aba salva no fim do boot achando que era entrada nova. Corrigido pra checar `window.top.performance` (a navegação de `index.html`, quem realmente sofre o F5). Commitado e pushado (`f480db5`), avisando antes.

## ▶️ Continuação 14/08/2026 (bloco 4) — 2º bug real de favicon no link solar, diferente do de 2.9

Retomada do bloco 3 abaixo (mesmo dia). Usuário reportou de novo o ícone genérico na aba ao abrir um link de compartilhamento solar. A correção anterior (2.9, `404.html`) resolveu um bug real mas não era este: `solar-compartilhado.html` — o arquivo que renderiza de fato, depois do redirect — usava caminho relativo (`assets/images/...`) nos 3 `<link rel="icon">`, que resolvia errado (`/solar/assets/images/...`, 404 silencioso) porque a página vive em `/solar/<token>/`. Trocado pra caminho absoluto (`/assets/images/...`). Validado ao vivo em produção via `javascript_tool` (não só "deveria funcionar"): os 3 `href` computados na página real batem com `https://wallacelira.com.br/assets/images/...`. Commit `6b8d7d6`, avisado o usuário antes de commitar/pushar, sem merge de bump `__V` pendente no momento do push. Não afeta regra de negócio nem domínio V2 — não requer atualização dos documentos do Google Drive.

## 🏁 Encerramento 14/08/2026 (bloco 3) — correções pontuais, causa raiz real do bug do Drive/`.git`, 2ª rodada de 10 agentes, seção nova + 4 bugs no link solar, relatório de fechamento em PDF (feature nova)

Continuação direta do bloco 2 abaixo (mesmo dia). Sessão longa, muitos pedidos pontuais do usuário intercalados com investigação de bugs reais — resumo em ordem cronológica.

**Correções pontuais no arranque do bloco**: link de compartilhamento solar falhando em sessões longas (`idToken` Firebase sem renovação — `renovarTokenFirebase()` nova, roda a cada 45min); piso absoluto do card 05 "Desemprego extremo" dessincronizado do card 03 (R$9.223,66 vs R$7.831,17, literal congelado desde 11/08 nunca atualizado quando Boletos/Consórcios mudaram) — 3 consumidores agora derivam da mesma fórmula ao vivo; linha "Consórcios" removida de onde é estruturalmente sempre R$0,00 desde a migração pros boletos; legenda gigante de extrato linha-a-linha da Caixa Mastercard_Infinite removida (ilegível, função de fetch órfã removida junto).

**CC-211/212 + Obrigações Operacionais + Emagrecimento**: as 2 únicas caixas da grade sem barra/%/meta ganharam meta = comprometido do ciclo (fatura MP / total dos 2 cartões, não teto de poupança). Seção "Obrigações Operacionais" dividida em 2 cards (o que soma no Total vs. contexto Wärtsilá que não soma), 4 legendas soltas viraram 1. Aba Emagrecimento ganhou o 4º card do kpi-strip (Altura 1,87m + IMC calculado).

**Achado de infraestrutura real, causa raiz do bug recorrente "worktree aparece toda deletada"**: ao rodar a 2ª leva de 10 agentes (ver abaixo), 3 das 10 worktrees vieram corrompidas de novo — mesmo padrão já visto em 13/08 e na manhã de 14/08. Desta vez investigado até a causa raiz de verdade: **45 arquivos `desktop.ini` estavam de fato commitados no repositório**, não só residindo no disco — toda limpeza anterior (`find .git -iname desktop.ini -delete`) era temporária porque nunca removia do índice do git, então o Drive recriava e o ciclo se repetia. Removidos do índice com `git rm --cached` + `.gitignore` novo. As 3 worktrees corrompidas desta sessão foram resolvidas do mesmo jeito das vezes anteriores: relatório do próprio agente identificou os arquivos reais alterados, lidos direto do disco (bypassando git) e reaplicados manualmente no `main`.

**2ª rodada de 10 agentes** (pedido explícito do usuário: "coloque 5 agentes e de designer de produto Senior... otimize para mobile, desempenho e estética máxima") — mesmo padrão da rodada da manhã (worktrees isoladas, sem commit/push/migration pelos agentes). Design: touch targets <44px corrigidos, `kpi-strip` responsivo, contraste WCAG AA, ~178 font-sizes migrados pros tokens, ~25 cores hardcoded trocadas por tokens reais, hover/focus estendidos. Performance: html2canvas (200KB) sob demanda em vez de bloquear o boot, preload nos 9 scripts que baixavam tarde numa cadeia em série, `font-size:16px` nos campos que disparavam zoom do iOS.

**Relatório de fechamento em PDF — feature nova completa** (pedido do usuário: "quero um botão de link para baixar relatório que possa ser gerado em pdf com um resumo e fechamento de tudo, você pode pegar essa atividade"). Escopo alinhado com o usuário via pergunta direta (Resumo Executivo + Obrigações Operacionais + Todas as Caixas + Balanço/Patrimônio + Reembolsos Wärtsilá, botão no topo do painel). Implementado em 2 partes: `src/relatorio/coletar-dados-relatorio.js` (dentro do iframe, lê o texto já renderizado — nunca recalcula) + geração do PDF em `index.html` (jsPDF sob demanda). 1ª versão só tinha texto corrido — usuário mandou print de um relatório corporativo de referência e pediu pra melhorar; reescrito com faixa de cabeçalho, KPIs em destaque e cards de seção coloridos. Testando o PDF real gerado (decodificado byte a byte, não só "rodou sem erro"), 2 bugs reais achados e corrigidos: emoji/seta virando mojibake (fontes padrão do jsPDF não suportam Unicode fora do latin-1 — sanitizador novo) e a seção "Todas as Caixas" saindo sempre vazia (formato de card diferente do resto do site, sem `.row` — extrator alternativo). **Não testado o fluxo completo ao vivo** (login não é possível neste ambiente) — pendência pra próxima sessão.

**Link de compartilhamento solar — seção nova + 4 bugs reais**, a parte mais testada da sessão (token real de produção, ao vivo, 3 rodadas de correção): adicionada a seção "📈 Crédito garantido vs. crédito em formação" (Fluxo 1 fechado + Fluxo 2 em formação, por casa) — a RPC já devolvia os campos necessários (`baseline_kwh`/rateios), não precisou mudar banco. Bugs achados testando ao vivo: "Fechado em undefined"/"Período: undefined" (campo vivia só no objeto pai, não chegava no objeto por casa); payback saindo 0,7 anos — achado do próprio usuário ("como vou pegar 14800 em 7 meses?") — causa real era dividir a economia acumulada desde a ativação pelos dias do ciclo GD atual, não da ativação real (corrigido, payback real 2,3 anos); "Geração hoje" mostrando dado de ontem depois da virada do dia (fuso Brasília, mesmo truque do painel privado, só não replicado aqui ainda); reestilizado pra ficar visualmente idêntico ao painel privado depois do usuário pedir "quero idêntico" e mandar prints comparando os dois. Também corrigido: ícone da aba genérico em links `/solar/<token>` (faltava o ícone SVG no `404.html`, só nesse arquivo) e o mesmo bug do investimento padrão R$25.000→R$14.800 que já tinha sido corrigido no painel privado, mas essa 2ª cópia (comentário já avisava que eram os "mesmos") tinha ficado pra trás — lição reforçada pelo próprio usuário: "lembre que tem mudança dessa que tem que fazer no site também".

**Caixa Emagrecimento sem saldo**: achado do usuário ("a caixa tem saldo") — criada em 12/08, depois da virada do ciclo, nunca recebeu o aporte mensal automático. Confirmado via SQL direto no banco (2 transações reais, saldo líquido R$0,00 por coincidência), lançado o aporte que faltou (`TX000318`, R$278,89) com autorização explícita do usuário.

**Perguntas respondidas, sem código**: relevância de dados de irradiância solar (Open-Meteo) pra previsão de geração (respondido, não implementado, fica de sugestão pro usuário decidir); variação hora a hora do autoconsumo (explicação física, não é bug).

Todos os commits desta sessão (cronológico, a partir de onde o bloco 2 parou): `7f20b18` (Firebase token) → `fe5def4` (piso absoluto) → `ba11e41` (legenda Mastercard_Infinite) → `b6e4b86` (CC-211/212 + Obrigações + Emagrecimento) → `bb2cc2a` (45 desktop.ini + .gitignore) → `13db593` (2ª rodada de 10 agentes) → `329a8a6` (botão PDF + investimento 14800) → `c25c46e` (Fluxo1/2 no link solar) → `86b0e14` (ícone 404.html) → `58d5026` (undefined + cor da barra) → `cef4617` (payback) → `a944d89` (Geração hoje + cards) → `4f77f2d` (PDF visual + emoji + Caixas vazia). Todos com merge do bump automático de `__V` entre cada um, sempre avisando o usuário antes de commitar/pushar. `TX000318` foi SQL direto (Supabase), não passa por commit git.

## 🏁 Encerramento 14/08/2026 (bloco 2) — Workflow de 10 agentes (performance + design premium), 6 commits publicados, boot 6,1s→2,5s

Continuação direta do bloco 1 abaixo (mesmo dia). Usuário pediu explicitamente ("coloque 10 agentes, 5 senior em programação e 5 designer gráfico, torne esse site profissional e premium") — antes de disparar, alinhado com o usuário via 3 perguntas: escopo visual (refino da identidade atual, não redesign amplo), escopo performance (client-side + backend/RPC liberado), isolamento (worktrees + revisão antes do merge). Rodado como `Workflow`, 5 agentes performance + 5 design, cada um em worktree isolada, instruídos a NUNCA commitar/pushar/aplicar migration.

**Resultado do workflow**: 9/10 concluíram. O 10º (`design-iconografia-hierarquia`) falhou ao criar a worktree com `fatal: ... git metadata that could not be resolved` — investigado e achado o problema real: **o Google Drive estava sincronizando a pasta `.git/` inteira**, injetando `desktop.ini` em toda subpasta, inclusive `.git/refs/`, quebrando a resolução de refs do Git. Mesma causa raiz de um bug de "worktree corrompida" documentado numa sessão anterior (13/08) que nunca tinha sido explicado até agora. Removidos todos os `desktop.ini` de dentro de `.git/` (~150 arquivos, seguro — resíduos do Explorer, não objetos git reais) — desbloqueou `git fetch`/`push` que também estavam falhando pelo mesmo motivo.

**Integração manual dos 9 resultados** (não foi um merge automático — cada worktree revisada e integrada com cuidado):
- 3 agentes de performance tocavam `app.js` — aplicados via `git apply --3way` sequencial, sem conflito.
- 4 agentes de design tocavam `assets/css/styles.css` — aplicados via `git apply --3way` sequencial, **2 conflitos reais** (2 agentes diferentes editando a mesma regra `.home-nav-btn`/`.cm-card` com propósitos complementares — sombra vs. cor) — resolvidos combinando as duas mudanças, não escolhendo uma só.
- 1 worktree (`perf-sweep-continuacao`) veio com o **mesmo bug de corrupção do Drive** — `git diff` mostrava 210 arquivos "deletados" (o repo inteiro). Descartado o diff automático; os 4 arquivos realmente alterados por esse agente foram identificados pelo próprio relatório, lidos diretamente do disco da worktree (bypassando git) e reaplicados manualmente no `main`.
- 1 agente (`perf-lazy-load-abas`) não fez nenhuma mudança de código — investigou e concluiu, corretamente, que lazy-loading das abas do Livro Razão quebraria os contadores "(N)" nos botões (dependem de contagem de `<tr>` no DOM, não de `VARS`), documentou a abordagem seg futura sem implementar às cegas.

**Bug real achado só na integração** (nenhum agente isolado o cometeu — só apareceu ao combinar): sintaxe de objeto (`chave: valor,`) dentro do corpo de uma `class` (`_CacheComTTL`, `app.js`) — `SyntaxError` quebrava o parse do arquivo inteiro, painel carregava a tela mas "não carregava nada" (relato do usuário). Corrigido convertendo pra sintaxe de classe.

**Publicação em produção, 6 commits sequenciais** (cada um com merge do bump automático de `__V` que o GitHub Action gera a cada push, sempre avisando o usuário antes de commitar/pushar):
1. `ec0b4e2`/`b1dec7e` (com merge) — todas as 9 mudanças do workflow integradas + fixes de hoje do bloco 1.
2. `8ea29e7`/`e96cf5b` — achado ao vivo (medição real em produção): `reconciliarTransacoesPluggy` tinha uma 4ª busca fora do `Promise.all`; contexto de dedupe (Onda 6/7) passou a rodar em paralelo com o fetch principal, não mais depois.
3. `90003c6`/`e96cf5b` — `inboxAdicionarItem()` tinha um lookup O(n) (`VARS.INBOX_FINANCEIRA.find`) rodando ~1.057x no boot — trocado por índice `Map` O(1).
4. `08a7665`/`bacf111` — instrumentação temporária de diagnóstico (breakdown de tempo dentro de Onda 6/7), pra investigar por que as correções 2-3 não mudaram o tempo medido.
5. `32c9c2c`/`614570f` — instrumentação removida, achado documentado: **não era mais bug de código**. O trabalho síncrono real dentro de Onda 6/7 é rápido (~130-140ms); o tempo de ~1,7-1,8s vem de essas 2 ondas começarem tarde (~1,2s após o boot) e disputarem o mesmo thread JS com as ~18 outras ondas concorrentes até o fim. Corrigir isso de verdade exigiria throttling de concorrência ou mover processamento pro servidor — fora do escopo de hoje, documentado como backlog.

**Resultado medido, com o usuário testando ao vivo em produção via DevTools** (não só localhost — usuário confirmou que o servidor local de teste estava inflando o número): boot caiu de ~6,1s (medido erroneamente no servidor PowerShell local, sem HTTP/2) pra **2.486ms reais em `wallacelira.com.br`**, com breakdown completo por módulo capturado via a instrumentação `window.WALLACE_BOOT_TIMING` (mantida permanente, não é diagnóstico temporário).

**Lição de processo**: a decisão de "investigar até o fim em vez de aceitar a 1ª hipótese" (usuário pediu "ataque" 2x depois de resultados que pareciam não ter funcionado) levou a descobrir que 2 das "correções" aplicadas (paralelização do contexto + índice O(1) da Inbox) eram legítimas mas não eram a causa real do gargalo residual — só ficou claro depois de instrumentar e medir de verdade, não adivinhar. Ambas as correções foram mantidas (não são erradas, só não resolveram o problema que motivou a investigação).

## ▶️ Continuação 14/08/2026 (bloco 1) — 2 correções pontuais achadas em auditoria de código, ainda não commitadas

Retomada da sessão de 14/08 abaixo (mesmo dia). 2 arquivos com correção pronta no working tree, pendentes de commit:

1. **`src/auditoria/verificacoes/auditoria-automatica.js`** — a checagem 7 (Reservas) somava `r.boletos` na conferência, mas `recalcularBalanco()` tinha parado de somar Boletos no total das Reservas em 13/08 (Caixa Boletos migrou de seção 05 pra 06/operacional). A checagem nunca foi atualizada, então acusava "1 divergência" (≈R$1.648,55, o valor da Caixa Boletos) em todo carregamento desde 13/08 — não era o bug do rodapé de novo (já corrigido em 2.3 do bloco anterior), era a própria fórmula da auditoria desatualizada. Soma corrigida pra 12 caixas (era 13).
2. **`src/solar/hydrate-clima-solar.js`** — `aplicarClimaSolar()` rodava sem proteção contra chamada repetida; `hydrate()` é disparado até 13x durante o boot (uma vez por caixa promovida V1→V2), cada uma refazendo a mesma requisição à Open-Meteo em série (limite de conexões por host), somando ~7s de atraso no fim da fila. Corrigido com flag de módulo que só busca 1x por carga de página (clima é só contexto visual, nunca usado em cálculo).

Nenhuma das duas mudanças afeta o que o Claude Chat precisa saber (sem domínio V2 novo, sem regra de negócio nova) — não requer atualização do Google Drive. Próximo passo: testar as 2 mudanças ao vivo e commitar (avisando antes).

## 🏁 Encerramento 14/08/2026 — Reconciliação Wärtsilä concluída, padronização de caixas, sync multi-conta MP, governança de documentação corrigida

Continuação direta da sessão de 13/08 abaixo (mesmo dia de trabalho, atravessou a virada de data). Trabalho dividido em 2 blocos.

**Bloco 1 — investigação Pluggy Mercado Pago (herdada de sessão anterior)**: pendência 1.1 do handoff anterior era "saldo reservado dos cofrinhos não aparece". Confirmado que não era bug (valor real é R$0). O problema real surgiu de lado: usuário tinha 2 contas Mercado Pago novas conectadas direto no portal `meu.pluggy.ai`, sem jeito de descobrir o `item_id` real pra colocar no secret. 7 caminhos técnicos tentados e descartados. No meio da investigação, **usuário excluiu a conta inteira da Pluggy por engano** (achou que era só o app de demo) — quebrou toda sincronização (não só Mercado Pago). Confirmado com suporte da Pluggy: irreversível. Usuário recriou a conta, gerou credenciais novas, e o método certo de achar `item_id` foi descoberto (dashboard → aba "Demo" → item conectado → topo do painel de detalhe mostra o `item_id` real — documentado no docstring de `sincronizar_pluggy.py` pra nunca mais precisar redescobrir). 5 conexões recuperadas e testadas com sucesso.

**Automação da reserva de cofrinho** (pedido do usuário, "nunca mais precisar avisar manualmente"): `extrair_reservas_pluggy()`/`lancar_reservas_pluggy()` novos em `sincronizar_pluggy.py` — varre transações "Dinheiro reservado/retirado X" das contas MP a cada sync, traduz pro `caixa_id` real via `CAIXA_PLUGGY_MAPA`, lança via RPC nova `lancar_reservas_pluggy` (idempotente, `pluggy_tx_id` UNIQUE). Cutoff em 13/08/2026 23:59:59 UTC pra não duplicar o que já tinha sido lançado manualmente na cascata Wärtsilä.

**Bloco 2 — auditoria completa das 6 abas + reconciliação Wärtsilä + recalibração + padronização**, o grosso desta sessão:
1. 2 workflows multi-agente (6 agentes cada, 1 por aba) auditando estética+legendas e depois fórmulas+matemática — ~60 achados reais, aplicados por aba em worktrees isoladas, merge de volta pro `main` (5 limpos, 1 com working-tree corrompido resolvido manualmente arquivo por arquivo).
2. **Reconciliação Wärtsilä ciclo 2026-07 fechada** — TED de R$6.682,76 recebido, mais R$340 de antes = R$7.022,76 de um bruto de R$7.362,76 (corrigido pelo usuário, não R$7.380,61 gravado). 2 erros reais cometidos e corrigidos na mesma sessão (detalhados no encerramento de 13/08 abaixo): LREI0003 quitada por engano com a perna errada (revertido, é de um ciclo anterior); pernas 2+4 mandadas pra Caixa Lance por engano (corrigido — cada cartão/serviço tem caixa própria: MP→"Mercado Pago", cartão corp pessoal→"Mastercard/Infinite"). Cascata final: TX000280-302, 3 caixas negativas cobertas como aporte (não empréstimo — distinção que o usuário fez questão de marcar), LREI0005 quitada com saldo próprio da Caixa Boletos, LREI0003 quitada com saldo ocioso disponível, LREI0004 e R$340 restante ficam pendentes (dependem de dinheiro que ainda não existe — R$340 confirmado ainda não recebido em 14/08).
3. **Recalibração de 17 caixas** pra valores reais informados pelo usuário (nova baseline, Pluggy alimenta daqui pra frente) + **renomeação de 3 caixas** pra evitar colisão de nome e bater com a nomenclatura da própria Pluggy: `Provisionado Wärtsilä`→`Caixa Wartsila` (distinta de "Fatura Wärtsilä", a obrigação real), `Mercado Pago`→`Caixa Mercado Pago`, `Caixa Mastercard/Infinite`→`Caixa Mastercard_Infinite`. CC-205 (Bens Duráveis) e CC-209 (Emagrecimento) atribuídos. FK em `v1_v2_caixa_mapa` limpa antes de renomear (dependência "mate a V1" — pedido explícito do usuário).
4. **Diretiva "V1 tá morto e enterrado, V2 é a verdade"**: removidas várias comparações V1×V2 residuais que ainda apareciam como aviso/badge no console/rodapé (Patrimônio Total, Provisionado Wärtsilä).
5. **Consolidação de metas**: os 12 cards estáticos de caixa + os dinâmicos passaram a ler `caixas.teto_mensal` como fonte única (antes, 4 vinham hardcoded no JS).
6. **Rodadas de polish visual** (várias voltas com screenshot do usuário): CC-codes nos títulos dos cards novos; barra/%/meta padronizados nos cards dinâmicos (antes bare); tooltip de composição ligado nos cards novos; legendas de "aporte alvo" movidas pra dentro do tooltip, deixando só %/meta no card; saldo negativo sempre vermelho com sinal de menos (passe centralizado no fim de `hydrateCaixas()`); botões de aba do Livro Razão padronizados em largura; contador "(N)" do LREM (Emagrecimento) corrigido (faltava no array `paineis`, mesmo padrão de bug já documentado 2x antes); seção "Livros razão" reposicionada pra ficar logo abaixo de "Controle caixa variável" (era seção 15, virou 07); cards sem meta usam `visibility:hidden` na barra em vez de omitir o bloco (2ª tentativa — a 1ª, omitir, deixou os cards com altura desigual, achado do usuário via screenshot).
7. **Bug do rodapé "N divergência(s) SSOT" que nunca zerava**: investigado por várias rodadas de screenshot do usuário mostrando o badge persistindo mesmo depois do dado já ter resolvido. Causa raiz: `auditoriaAutomatica()` roda 3x por carregamento, e nenhuma das 2 branches (0 problemas / N problemas) removia o aviso anterior do rodapé antes de decidir o que mostrar — corrigido com um `.remove()` no início dos dois branches. Rebase falso-positivo no push (conflito aparente era só o commit automático de bump do `__V`, não conteúdo real).
8. **14 transações de calibração categorizadas** pelo próprio usuário mesmo dia (confirmado 14/08: todas as 27 transações `origem='reconciliacao'` de 13/08 já tinham `categoria_id`, nada a fazer).

**Sync multi-conta Mercado Pago**: `mercadopago_sync.py` reescrito pra aceitar `MERCADO_PAGO_ACCESS_TOKEN` como lista (`token1,token2`), checkpoint isolado por `collector_id`. Nova aplicação de produção criada na conta 2 (`wallace.servidor@gmail.com`) via Browser pane — usuário resolveu os 2 reCAPTCHA e o 2FA que apareceram no fluxo (agente não pode/deve resolver captcha). Access Token revelado e repassado pro usuário colar no GitHub Secret (agente nunca digita token em campo de terceiro, regra de segurança). Testado ao vivo: conta 1/2 sync incremental (7 eventos), conta 2/2 primeira sincronização completa (3 eventos) — sucesso confirmado no log do workflow #45.

**Data-limite em metas**: usuário informou 2 datas (Escola de Júlio 01/11/2026, Aniversário Júlio 25/08/2026) sem o sistema ter campo pra isso. Coluna nova `caixas.meta_data_limite`, badge de contagem regressiva no card ("faltam N dias", vermelho se ≤7). Validado ao vivo no browser.

**Rendimento por cofrinho Mercado Pago — pendência fechada como bloqueio confirmado, não abandonada por falta de tentativa**: usuário pediu pra investigar mais a fundo antes de desistir da automação (tinha aceitado "manter manual" só depois de ver a investigação séria). Confirmado 2x com a documentação oficial da Pluggy (`docs.pluggy.ai/docs/accounts.md` via índice `llms.txt` — a 1ª tentativa de busca trouxe uma resposta que parecia promissora, sugerindo um campo `remuneration`/CDI, mas não se confirmou ao re-consultar a URL certa da doc — provável alucinação da ferramenta de busca, corretamente descartada antes de reportar como fato ao usuário) que `bankData` não documenta nenhum campo de juros/remuneração pra `reservedBalances`. Decisão final: manter calibração manual mensal.

**Auditoria e correção da governança de documentação** (motivada por: "to tento muito problema para o claude chat atualizar as coisa, ele se perde todo"): achado real, não hipotético — o `MANUAL_OPERACIONAL_AGENTES.md` (seção 11.5) apontava pra uma pasta do Google Drive (`Sistema Wallace Lira - Claude Chat/`) que tinha sido abandonada em 09/08 e estava **vazia**; a pasta real (`Livro Razão/Agentes/`, confirmada pelo próprio `ONDE_LER.md`) tinha uma cópia do manual **5 dias desatualizada** (ainda dizia que `wallace_dados` era fonte viva, desligada há 2 dias) e um `CUSTOM_INSTRUCTIONS_SISTEMA_WALLACE (1).md` — nome com sufixo numérico, violando a própria regra de manutenção do `ONDE_LER.md`, sinal de sincronização quebrada. Corrigido: seção 11.5 do manual atualizada com o endereço certo + o achado registrado; cópia do manual no Drive sobrescrita; `CUSTOM_INSTRUCTIONS_SISTEMA_WALLACE.md` reescrito (sem sufixo, cobrindo tudo que mudou até 14/08) e o arquivo "(1)" antigo apagado; um `.gdoc` órfão solto na raiz do Drive (resíduo de uma tentativa antiga com Google Doc) também apagado. **Checklist de Encerramento de Sessão (seção 10 do manual) endurecido** para exigir explicitamente os 2 arquivos do Drive daqui pra frente — antes só citava os 2 arquivos deste repositório, e foi exatamente esse ponto cego que causou os 5 dias de defasagem. Pedido explícito do usuário: "quando eu pedir para atualizar a troca de turno, tem que atualizar todos os documentos do projeto, inclusive o que o chat usa para se atualizar" — esse pedido agora é a regra permanente da seção 10, não fica só nesta conversa.


---

**Blocos anteriores a 13/08/2026** foram movidos pra `docs/changelog/PASSAGEM_DE_TURNO_ARQUIVO_ATE_13082026.md` em 15/08/2026 (achado de UX/documentação da auditoria de 43 especialistas: este arquivo tinha passado de 1500 linhas nunca arquivadas). Nenhum conteúdo foi perdido, só realocado — consultar o arquivo morto se precisar de histórico de sessões anteriores a 13/08.
